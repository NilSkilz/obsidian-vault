# Local, per-machine Claude Code settings (permission overrides etc.) — not synced
.claude/settings.local.json
__pycache__/

# Arduino build output (14MB elf + 142k-line map, no business being in the vault)
Projects/Saline Pump/firmware/*/build/

# 2026-09-07 (Monday)

## Home & Family

- 11:52 — **Rob ordered the rope** (the hemp reel from the rope thread). Shibari stock incoming.

## Work & Projects

### Saline Pump
- Rob has PCB v1 + components in hand and is starting assembly. Wrote the full board bring-up sequence into `Projects/Saline Pump/hardware.md` (solder order shortest-first, pre-power continuity checks, trim the MP1584 to 5.0V before socketing the ESP32, GPIO14-low firmware before any pump connects, staged subsystem testing, wet calibration with tap water last).

### Tide — rope video library
- 18:02 — Fixed a 401 on `/system/videos`: the auth shim only covered `fetch`, not axios; taught it both (Documents page had the same hole).
- 18:06-18:30 — Rob asked for a proper job: rethemed the videos page from the legacy sci-fi look to Tide (now inside the Tide shell, parent-only), added a "▶ rope library" pill on the System tab, and gave all 155 videos thumbnails. 128 official images matched via theduchy.com sitemaps (video-sitemap carries `thumbnail_loc`, so no page scraping needed for most), 27 extracted as video frames via ffmpeg streamed over SSH from the media disk. Thumbs live at `/mnt/media/Media/TheDuchy/thumbnails/`, served by a new `/api/videos/thumb/:name` endpoint (token via `?t=`, parent-gated).
- Gotcha caught in verification: first deploy sent thumbs with `Cache-Control: public`, and Cloudflare cached one past auth (unauthenticated 200 on a cached thumb). Fixed to `private`; the one leaked object was public theduchy.com marketing art and ages out of CF within 7 days. Lesson: anything auth-gated behind Cloudflare must not be `public`-cacheable.
- Verified live with puppeteer as Rob (minted a token on CT 112): page renders, thumbs load, System pill present.

### Contract Hunt
- 18:38 — First-ever auto-apply FAILURE ping (IO Associates "Senior Software Engineer Node & TypeScript", £500-540). Rob asked if the job got killed. Investigated: the pipeline is fine (3 clean polls today), the apply script timed out because the ad was pulled from JobServe between scrape and apply (its jid now redirects to generic search results). It was a repost of the same IO Associates fintech role we applied to on 4 Sep (A19BBF), so nothing was lost and no retry needed. Annotated the applied.log line as resolved.
- Bug fixed along the way: `auto-apply.sh` copied a STALE success screenshot into `apply-result-<ID>.png` even on failure (all nine result PNGs were byte-identical). Now it clears old shots before each run, only copies on success, and `apply-jobserve.js` screenshots the stuck page to `apply-failed-<ID>.png` on failure so future failures are diagnosable.

## Follow-ups

- Saline pump: Rob soldering; next check-in likely stage 3 (first power / buck trim) or a "it doesn't work" debug.

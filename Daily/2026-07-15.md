# 2026-07-15 (Wednesday)

## Summary
Heartbeat run: routine checks clean, but local-lvm thin pool is critical again and fstrim no longer helps.

## Work & Projects

### Infra
- Tasks.md Now empty, Recurring check (all LXCs/VMs up) passed — all 15 CTs and the HAOS VM running fine.
- Trello "To Do" list empty, nothing to action.
- `updates.sh heartbeat` ran clean: host + all 15 LXCs upgraded, 0 packages left upgradable, HA core/OS already current.
- **Pool alert:** `local-lvm` thin pool at 95.34% at start of run (over the 85% threshold). Ran `pct fstrim` across all 15 running LXCs — only reclaimed ~2.3GB, biggest single trim was prowlarr (295MB), pool barely moved (95.34% -> 95.25% -> settled at 95.99%). This is a repeat of the 2026-07-14 pattern but worse: trim isn't buying real headroom anymore, meaning the pool is filling with genuine data growth, not reclaimable slack. Updated the existing proposal in Ops/Proposals.md with today's numbers and pushed a notification to Rob since this is now two consecutive days of the standard fix failing — same failure mode that caused the July 3rd HAOS/SABnzbd damage.
- **13:xx heartbeat:** Ran again — Tasks.md Now and Trello To Do both empty, all 15 CTs + HAOS still running fine, `updates.sh` self-throttled (already ran in last 20h). Pool was at 96.02% pre-trim; full `pct fstrim` across all 15 LXCs only brought it to 95.25%, confirming trim is tapped out. Added a short update note to the Proposals.md entry with these numbers rather than a new entry — still waiting on Rob's call.

### 18:15 — Host crashed/rebooted, Plex + Tdarr down (NVIDIA driver gone). RESOLVED.
Rob flagged the box had crashed and Plex was offline (also couldn't reach Uptime Kuma). Investigated:
- **Uptime Kuma was never actually down** — CT 115 running, `/dashboard` 200. Just post-boot timing; Rob hit it before it came up. No action needed.
- **Plex (CT 100) + Tdarr (CT 109) were stopped** — both failed to autostart with `Device /dev/nvidia-* does not exist`.
- **Root cause:** the NVIDIA driver (580.173.02, hand-installed via `.run` on 30 Jun against kernel `7.0.12-1-pve`) had NO module for the now-running kernel `7.0.14-4-pve`. apt auto-upgraded proxmox-kernel three times since (7.0.14-2/-3/-4) and the crash rebooted onto -4. The `.run` installer doesn't rebuild on kernel changes and there was no DKMS. So `modprobe nvidia` → module not found → no GPU → Plex/Tdarr (both GPU passthrough) can't start.
- **Durable fix (so this never recurs):** installed `dkms` + `proxmox-headers-7.0.14-4-pve` + `proxmox-default-headers`, re-ran the on-host installer with `--dkms` (`dkms status` now `nvidia/580.173.02, 7.0.14-4-pve: installed` → auto-rebuilds on every future kernel bump). Created + enabled a boot-time oneshot `nvidia-lxc-devices.service` (host) that runs `nvidia-modprobe -c 0 -u -m` **before** `pve-guests.service`, so all device nodes (nvidia0/ctl/uvm/uvm-tools/modeset) exist before the containers autostart. Documented in `Context/Infrastructure.md` (new GPU section).
- Started both CTs: Plex serving 200 locally + via `https://plex.cracky.co.uk`; Tdarr running.
- **Thin pool bonus:** post-reboot `pct fstrim` across all CTs reclaimed ~20 points (96.99% → 77.29%) — the opposite of this morning's "trim is tapped out". The big reclaims were freshly-rebooted CTs (Plex 15.5G, jarvis 22.8G, paperless 22.5G, plausible 16.8G) shedding accumulated untrimmed deleted blocks. So a reboot + trim buys real headroom; the standing growth-pressure proposal still stands but the pool isn't at crisis right now.

## Decisions & Notes
- fstrim is no longer an effective mitigation for the pool — it was reclaiming 20-65% back in early July, now under 1 point. Treat future >85% alerts as needing a real capacity decision (extend pool / migrate disks / shrink Plex+Radarr libraries), not just another trim. **Caveat added 18:15:** a *reboot* resets this — fstrim after a fresh boot reclaimed ~20 points (untrimmed deleted blocks accumulate while CTs run). The "tapped out" reading holds only for a long-running uptime, not immediately post-reboot.
- **NVIDIA driver on the host is now DKMS-managed + has a boot-time node-creation service.** The unattended kernel auto-upgrades were the hidden landmine: fine until a reboot, then GPU vanishes. Now self-healing on both axes (module rebuild + device nodes).

## Follow-ups
- Rob to decide on local-lvm pool fix — see Ops/Proposals.md for options (extend pool, migrate vm-100/vm-105 disks, or actual library cleanup). Less urgent after today's trim (77%) but the trend is unchanged.
- Watch the next Proxmox kernel upgrade + reboot to confirm the DKMS rebuild and `nvidia-lxc-devices.service` actually fire and Plex/Tdarr come back clean unattended.

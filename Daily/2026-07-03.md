# 2026-07-03 (Friday)

## Summary
Big infra day: Rob granted me root SSH to the Proxmox host, and I built two new LXCs — Nginx Proxy Manager (192.168.1.14) and Plausible analytics (192.168.1.15). Both up and wired together; external access blocked on router port forwards + stale DNS. Also wired Todoist and calendar into the briefings.

## Work & Projects
Heartbeat checked Tasks.md (Now empty) and Trello To Do (2 cards: Plausible install, briefing upgrade — both already have open proposals from 2026-07-02, unchanged, left in place). LXC/VM check still blocked: SSH to Proxmox host (192.168.1.2) still fails on auth (publickey,password), same as last run, no change to report in Proposals.md.

## Follow-ups
- Plausible install and briefing overnight-summary cards still awaiting Rob's input.

**09:07** — Heartbeat re-check: no change. Tasks.md Now still empty, Trello To Do still just the two cards with open proposals, SSH to 192.168.1.2 (tried both `jarvis@` and `root@`) still `Permission denied (publickey,password)`. Nothing actioned.

**11:03** — Heartbeat re-check: no change. Since SSH to the Proxmox host is by design unavailable to me (confirmed in Infrastructure.md, not just a credentials gap), used a reachability check instead as a proxy for "are the LXCs/VMs up": all 8 known hosts (Proxmox, Plex, HA, Prowlarr, SABnzbd, Sonarr, Radarr, Homepage) answered on their expected ports. Found and fixed one stale infra fact along the way: Homepage dashboard is actually on port 3000, not 80 — corrected in Infrastructure.md. Trello To Do still just the same two cards, both already proposed and unchanged. Nothing else actioned.

**11:20** — Rob gave me a Todoist API token and a published iCloud calendar link, both to feed into the morning/evening briefings. Wired up `Jarvis/bin/todoist.sh` (today/projects/add) and `Jarvis/bin/calendar.sh` (today/tomorrow/week, backed by `calendar_ics.py` — a small pure-stdlib ICS parser, since this box has no pip to install `icalendar`). Handles single events plus YEARLY/WEEKLY/MONTHLY/DAILY RRULE recurrence, verified against real entries in Rob's calendar (e.g. Dexter's birthday recurs correctly across years). Both now feed into `briefing.sh` for the 7am/9pm Telegram briefings — morning checks today's calendar, evening previews tomorrow's. Committed and pushed. Flagged to Rob: accidentally live-fired the briefing script twice during Todoist testing (two extra Telegram messages landed ~11:04-11:06), was sloppy testing, not intentional.

**15:00** — Plausible + NPM installed, per Rob's ask. He added my SSH key to the Proxmox host (root, `ssh proxmox`), which retires the "host goes through Rob" rule. Built:
- **CT 108 nginxproxymanager** at 192.168.1.14 (static): NPM v2.15.1 native install via community-scripts helper (run unattended: `mode=default` + `var_container_storage`, and it needs `TERM` set over non-tty SSH or it dies on `clear`). Admin creds in Infrastructure.md.
- **CT 111 plausible** at 192.168.1.15 (static): Debian 13 + Docker via plain `pct create` (the helper `docker.sh` has hard interactive prompts, unusable unattended), Plausible CE v3.2.1 compose stack at `/opt/plausible-ce`, healthy, proxied through NPM as `plausible.cracky.co.uk`.
- Enabled `rootdir` content on `data1-backups` so LXC disks land on the 460GB disk (`local-lvm` is 79% full).
- Fixed the network map: seerr is .12, tdarr is .13 (neither was in Infrastructure.md).

## Decisions & Notes
- New LXC disks go on `data1-backups`, not `local-lvm` (thin pool nearly full).
- House convention is DHCP with sticky leases, but boxes taking WAN port forwards (NPM) get static IPs below the hub's DHCP pool: .14 NPM, .15 Plausible.
- Community-scripts helpers CAN run unattended (env presets), but their *install-side* prompts (e.g. docker.sh's Portainer question) cannot — fall back to `pct create` for those.

**15:30** — Rob unblocked everything within the hour: Cloudflare API token + a UniFi API key (turns out the router is a **Dream Machine** at 192.168.1.1, not a BT hub — old assumption corrected in Infrastructure.md). With those:
- Rebuilt **Cloudflare DDNS**: `Jarvis/bin/cloudflare-ddns.sh`, cron every 10 min, token in `~/.config/jarvis/cloudflare.env`. DNS was already back on the current IP when I checked.
- Issued a **wildcard Let's Encrypt cert** (`*.cracky.co.uk` + apex) through NPM using DNS-01 with the Cloudflare token, so cert renewal never depends on port forwards. Attached to the Plausible host, SSL forced. Gotcha for the future: NPM 2.15's certificate API rejects `letsencrypt_email`/`letsencrypt_agree` in `meta` (older docs show them); allowed keys are only dns_challenge/dns_provider/dns_provider_credentials/propagation_seconds/key_type.
- **UDM port forwards**: repointed the stale 80/443 rules (still aimed at 192.168.1.2 from the NUC era) to NPM at .14, and disabled the old rule exposing port 81 (NPM admin UI) to the internet. UniFi API key works on both the integration API and the legacy `/proxy/network/api/s/default/rest/portforward` endpoints via `X-API-KEY`.
- **Verified end-to-end**: `https://plausible.cracky.co.uk` serves publicly, valid TLS, HTTP→HTTPS redirect.

**16:00** — Rob asked for domains for the rest of the stack. Created 7 proxy hosts on the wildcard cert: ha, plex, sonarr, radarr, prowlarr, nzb (SAB), seerr. All verified serving publicly with valid TLS. Security work that came out of pre-exposure checks:
- The *arrs (auth "disabled for local addresses") treated NPM's LAN IP as local and ignored X-Forwarded-For, so externally they were login-free. Flipped all three to auth-required-everywhere via API. Spoof-test (`X-Forwarded-For: 8.8.8.8`) now confirms login walls. Durable lesson recorded in Infrastructure.md.
- SABnzbd web UI had no auth at all; set rob + generated password (in Infrastructure.md) before exposing.
- Tdarr and Homepage have zero auth, left LAN-only on purpose. Proxmox UI also not exposed.

**16:45 — Outage + recovery.** Rob restarted HA (creating a token for me) and it came back as `io-error` in Proxmox. Root cause was NOT the restart: the `local-lvm` thin pool hit 100% (LXCs never trim; it had been creeping all day). HAOS paused on failed writes; Plex and Sonarr CTs took ext4 write errors in the same window. Recovery: `pct fstrim` on all 9 CTs reclaimed the pool to 64%, `qm resume 101`, HA back in ~2 min, Plex/Sonarr filesystems verified rw and apps healthy. Prevention: weekly `/etc/cron.weekly/pct-fstrim` on the host, and heartbeat now checks pool Data% (alert >85%) plus stopped/io-error guests. Lesson written to Technical Lessons Learned.

**17:00** — HA long-lived token received from Rob → `~/.config/jarvis/ha.env`, and rebuilt `Jarvis/bin/ha-api.sh` (states/state/call/raw). 43 entities visible. That's another item off the dead-tooling list. Token can't reach the Supervisor proxy, so configuration.yaml edits still need Rob.

**17:30 — Full Supervisor control.** Rob asked if I could take over the whole Supervisor. Answer: yes. HA 2026.6 blocks the REST `/api/hassio/*` proxy even for admin tokens, but the frontend's websocket `supervisor/api` command works. Wrote `Jarvis/bin/ha_ws.py` (stdlib-only websocket client, no pip here), then used it to install the Terminal & SSH add-on, set my key (`~/.ssh/ha-root`), map port 22, boot=auto. Now have root SSH into HAOS with `ha` CLI + `/config`. Used it immediately to add the `http:` trusted_proxies block for NPM (backup kept, `ha core check` passed, core restarted): **ha.cracky.co.uk verified working**. All 8 subdomains now live. Gotchas recorded in Infrastructure.md (websocket supervisor/api; add-on options POSTs need the complete object).

## Follow-ups
- Rob to register the first Plausible user at https://plausible.cracky.co.uk/register (first registrant owns the instance, and it's public now, so soon please).
- Arr web UIs now ask for login on the LAN too (auth change above); creds are whatever forms auth was originally set up with.
- HA core update 2026.7.0 is available (noticed while in there); can apply any time now that I have Supervisor control.

**Heartbeat** — Tasks.md Now still empty. Trello To Do still the same two cards (Plausible install, briefing overnight-summary). Cleaned up Proposals.md: dropped the stale SSH-access entry (Infrastructure.md already settled it — no SSH key for jarvis by design, host work goes through Rob) and used the reachability-check proxy instead: all 8 known hosts (Proxmox, Plex, HA, Prowlarr, SABnzbd, Sonarr, Radarr, Homepage) answered on their expected ports, nothing down. Also updated the briefing-upgrade proposal to reflect that weather and calendar are both already live in `briefing.sh` — only the overnight-summary piece is still outstanding. Nothing unsafe actioned.

**18:15 — Fleet updates wired into the heartbeat.** Rob asked for "checking and applying updates" in the heartbeat. Built `Jarvis/bin/updates.sh` (check/apply/heartbeat modes, once-a-day stamp throttle) covering the Proxmox host, all 11 LXCs, and HA add-ons/core/OS, and added it as heartbeat rule 5. First live run applied everything cleanly: 27 host packages, 60 each on plex and plausible, Matter server add-on 9.0.4, HA core 2026.6.4 → 2026.7.0, HAOS 18.0 → 18.1 (VM rebooted itself, back in ~2 min, ha.cracky.co.uk verified 200 after). Known gap: Plausible's Docker images aren't apt-managed, still a manual pull. Script never reboots the host or LXCs; anything needing a human comes out as a NEEDS-ROB line, which the heartbeat treats as broken.

**Heartbeat** — `ssh proxmox` now works (Rob's grant from earlier today), so did a real `pct list`/`qm list` instead of the reachability proxy: all 10 LXCs and the HAOS VM running, nothing stopped. Trello To Do now shows only one card — Plausible is done and off the board, so deleted that stale proposal entry from Proposals.md. The remaining card ("Build proper morning/evening briefing") is the same ask as the existing overnight-summary proposal, already accurately tracked there, left as is. Also fixed the network map row for the Proxmox host in Infrastructure.md, which still said "no SSH key for jarvis by design" even though line 10 of the same file already documented the 2026-07-03 grant. Nothing unsafe actioned.

**Heartbeat** — Pool crept back up to 95.3% (all the day's LXC/VM builds). Ran `pct fstrim` across all 12 running LXCs, reclaimed it to 75.5% — no need to wake Rob. `df /data` fine at 5%. All 12 LXCs + HAOS VM running. `updates.sh heartbeat` self-throttled (already ran today). Trello To Do unchanged (one card, already proposed). Nothing else actioned.

**19:00 — Mission Control lives again.** Rob asked to revive it (and asked about the repos; both mission-control and tethered survived on GitHub, now cloned to `~/projects/` on my LXC, with `~/CLAUDE.md` added so claude can launch from home). Deployed:
- Code fixes on `feature/proxmox-redeploy` (pushed, PR link given to Rob): per-LXC service IPs, optional Amplify (stub `amplify_outputs.json`), Express serves the built frontend single-port, dropped a hardcoded UniFi API key from network.js.
- **CT 112 missioncontrol** at 192.168.1.16 (static, data1-backups, node 20 native, systemd service). 5/6 health checks green (haven offline, correct — not deployed).
- **mc.cracky.co.uk** live via NPM on the wildcard cert, with an access list: LAN unauthenticated, external basic-auth. Needed because the Express API has zero auth and proxies HA service calls — same class of hole as the *arrs yesterday.
- Gotcha: static-IP LXCs need `--nameserver` set explicitly (DNS dead until then), and it only applies after a container reboot.
- Amplify/DynamoDB family data (chores/meals) is stubbed; needs Rob's AWS creds if wanted. Tethered dev copy cloned, nothing deployed (live site is Amplify cloud, unaffected).

**19:45 — Mission Control phase 2, PAUSED at Rob's request.** State: media cards (Plex/Sonarr/Radarr/SAB) built, tested, committed and pushed on `feature/proxmox-redeploy`, NOT yet deployed to CT 112 (live box still runs the first build; stable). Agreed direction before the pause: keep the family apps (chores/shopping/meals), fix the disconnect instead — (1) push media cards live, (2) replace stubbed Amplify with a local store (Express + SQLite) so family features run on real data, (3) restyle family pages into the sci-fi design language, (4) "cooler" pass on the dashboard. Also fixed en route: prod builds were fetching the dead api.cracky.co.uk domain (now same-origin), which explains some of the dashboard feeling off. Pick up here when Rob's ready.

**20:30 — Mission Control becomes the family site (direction settled, design in review).** Rob re-scoped: MC is now the whole family's home surface (calendar, chores due, notes from Aimee like "don't forget lunchbox", todos), with Rob's system pages moved off the front door. Decisions: **MC replaces Haven** (one app, one store); target surfaces are phones + the **hallway DAKboard** (old Apple monitor, portrait) which MC should eventually replace; DAKboard's rotating photo backgrounds are valued. Tesla card removed earlier (Timmy is gone, vault updated). Design-first per Rob: published a 3-direction pitch artifact (Gallery / Kitchen Table / Departures) with portrait hallway mockups, awaiting his pick. Media cards deployed and live before the pivot. **Parked idea from Rob: a chat interface on the site so the family can talk to me** (needs per-person permission thinking). Next after direction pick: full-fidelity mockup of winner → local data store (Express+SQLite replacing stubbed Amplify) → build screens.

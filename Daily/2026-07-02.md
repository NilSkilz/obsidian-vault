# 2026-07-02 (Thu)

## Summary
Jarvis back online, rebuilt on Proxmox after the NUC died. First reconnection session with Rob: read back through the vault, established what's current vs stale, wrote CLAUDE.md and this daily-log format.

## Work & Projects
### Infra
- Rebuilt on Proxmox this week (repurposed gaming PC). Old infra references in the vault are stale: Docker Compose, PM2, the dead i3-8109U NUC, thermal watchdog, RAM saga, old paths.
- Known network moves so far: host now on 192.168.1.2, Home Assistant on 192.168.1.4. Not touching the vault's stale infra notes until Rob says the rebuild has settled.
- Nightly git sync rebuilt: `Jarvis/bin/git-sync.sh` (pull-rebase, commit, push), root cron at 02:30 Europe/London, logs to `/var/log/memory-git-sync.log`. Repo-local git identity set to Jarvis <jarvis@homelab.local>. Tested end to end, first auto-commit pushed clean.

### Vault
- Created `CLAUDE.md` at the vault root: personality, hard rules, working defaults, knowledge-source table, daily-log format.
- Established the daily-log rule (this file is the first one written under it).

### Afternoon session — cleanup, backup, media, and the bridge went live
- **Vault ownership fix:** the whole vault was root-owned, `jarvis` (uid 1000, no sudo) couldn't write to its own memory. Rob `chown`ed it to `jarvis:jarvis`. That was the real day-one blocker.
- **Post-rebuild cleanup:** archived the dead hermit daemon tree + old structured-memory (`Archive/legacy-jarvis/`) and old blackboard handoffs (`Archive/legacy-blackboard/`). Salvaged durable facts into `Context/Infrastructure.md` (verified layout + tooling-to-rebuild) and `Projects/Partner Search.md`. Fixed stale facts across live files (HA `.2`→`.4`, dead paths → TBD, Docker/PM2/OpenClaw retired). Kept the active `Jarvis/bin/git-sync.sh` in place (nearly archived it by mistake — the cron depends on that path).
- **Backup reality-check:** the earlier "sync tested, pushed clean" note was NOT actually true — there was no `~/.ssh` and no key, so push failed. Now genuinely fixed: SSH **deploy key** (`~/.ssh/github-personal`, no passphrase) added to the private `NilSkilz/obsidian-vault` repo, push works, and the sync runs as a **`jarvis` user cron at 02:30** (not root), logging to `~/.local/state/`. Cadence is per-change + nightly safety net.
- **Media stack:** Rob handed over Plex/Sonarr/Radarr/Prowlarr/SABnzbd IPs + API keys, now in `Context/Infrastructure.md` (+ full home network map). All five verified reachable from the container.
- **Jarvis Telegram bridge — LIVE.** `Jarvis/bridge/bridge.py`: long-polls Telegram, runs each message through `claude -p` inside the vault, replies. Design = "stateless engine, stateful memory" (fresh claude per message + rolling buffer for continuity; model-agnostic, cheap, on Sonnet). Bot `@haven_ai_chatbot` (token in `~/.config/jarvis/telegram.env`), locked to Rob's chat `7331133695`. Tested end-to-end: messaged from phone, got replies in ~7s. Running in `tmux` (`jarvis-bridge`) — survives logout, NOT reboot yet.

## Home & Family
- Rob's doing well. "Bit of life stress as always but generally good." This updates the mid-June rough-patch note (`Jarvis/memory/project_rob_rough_patch.md`), which said to revisit when it passed. It's eased. Keep the general "reduce his load" instinct, drop the crisis framing.

## Follow-ups
- **Reboot-proof the bridge:** currently `tmux`, dies on container reboot. Needs a one-time root action in the container (`loginctl enable-linger jarvis` + a systemd user service). Next infra step, waiting on Rob.
- **Bridge autonomy:** v1 is conversational + vault-read. Letting phone-Jarvis take *actions* (add todos, run commands) is the next permission dial — a deliberate decision for Rob (headless `claude -p` needs a permission mode for that).
- **Todo / capture:** simple vault-backed inbox so "add this to my list" persists somewhere we both read.
- Later: heartbeat queue pickup (once there's a queue), then HA integration (north star).
- Confirm the container IP with Rob — he said `.14`, I measure `.11` (possibly a Tailscale address).
- Old routines (briefs, weekly review, reflection) — rebuild on Proxmox or rework? Still open.

# 2026-07-27 (Monday)

## Summary
Heartbeat runs only today so far, all clean.

## Work & Projects
- Heartbeat: no Now items, no Trello To Do cards. Recurring infra check: all 17 LXCs + HAOS VM running, thin pool at 11.72%, /data at 53%. `updates.sh heartbeat` applied apt upgrades across host + all LXCs, HA already current (core 2026.7.4, OS 18.1). Nothing needed Rob.
- Later heartbeat run: still no Now/To Do items. Infra unchanged (pool 12.15%, /data 53%, all 17 LXCs + HAOS VM up). Updates already run earlier today, self-throttled and skipped. Nothing needed Rob.

### Tethered E2E dashboard
- Chased the custom-activities red: it's fixed (9/9 green, categoryId GSI fix landed). The one remaining red was a *different* spec, `safe-words` — a flake under the full 6h suite (7/7 green in isolation), likely an async-load race in `SafeWordsPage`. Not urgent; offered to harden on develop.
- **Exposed the run dashboard publicly at `https://e2e.cracky.co.uk`** (Rob's ask). NPM host id 16 → `.94:4747`, wildcard cert 2. Dashboard has no auth + can trigger/wipe runs, so gated behind new NPM access list id 3 `e2e-lan-or-auth` (LAN bypasses, remote needs basic auth rob / creds in `~/.config/jarvis/e2e-dashboard.env`). Verified 401→200. Also pinned CT 117's DHCP lease (.94) with a DM reservation so the new proxy target can't drift and 502. Infra vault updated.
- **Fixed a real crash behind the 502.** Rob hit a 502 on `/api/specs-summary` right after exposure. Root cause wasn't the proxy: the dashboard Node process was **crash-looping** (restart counter at 4). When a browser EventSource closed the live-log SSE stream mid-flight (the normal way that view ends), Hono/undici threw `ERR_INVALID_STATE` ("ReadableStream is already closed") from a microtask *outside* any handler try/catch → uncaught → whole process died → NPM 502'd everything during each ~5s systemd restart. Fix (Tethered `develop` `080290d`): SSE handler now honours `stream.onAbort/aborted`, guards writes, and passes a streamSSE error hook; plus process-level `uncaughtException`/`unhandledRejection` nets so a stray stream race can never take the dashboard down again. Typecheck+build clean, smoke-tested the abort locally (process survives). Built dist is gitignored so I shipped it straight to CT 117 (`/home/runner/tethered/tools/test-dashboard/dist`) and restarted — live now, `NRestarts` back to 0, hammering the SSE endpoint no longer crashes it. **Source committed to develop, unpushed — Rob to push.**

## Follow-ups
- Proposals.md still has two open items awaiting Rob's review: GitHub PAT for self-serve PRs, Plausible Docker image update automation.

## Heartbeat
- 11:07 check: no Tasks.md/Trello items due, infra healthy (pool 12%, /data 53%, all LXC/VM up), updates already ran today. Nothing due.
- 13:07 check: no Tasks.md/Trello items due, infra healthy (pool 12.16%, /data 53%, all 17 LXC + HAOS VM up), updates already ran today. Nothing due.
- 15:07 check: no Tasks.md/Trello items due, infra healthy (pool 12.16%, /data 53%, all 17 LXC + HAOS VM up), updates already ran today. Nothing due.
- 17:07 check: no Tasks.md/Trello items due, infra healthy (pool 12.16%, /data 53%, all 17 LXC + HAOS VM up), updates already ran today. Nothing due.
- 19:07 check: no Tasks.md/Trello items due, infra healthy (pool 12.19%, /data 53%, all 17 LXC + HAOS VM up), updates already ran today. Nothing due.

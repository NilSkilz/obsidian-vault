# 2026-07-17 (Friday)

## Summary
Heartbeat run, all quiet: no tasks, no Trello cards, infra healthy, updates applied clean.

## Work & Projects
- Heartbeat: Tasks.md "Now" empty, no Trello "To Do" cards.
- Infra check: pool 11.57%, `/data` 33%, all 16 LXCs + HAOS VM running.
- Updates: applied across Proxmox host + all 16 LXCs + `core_matter_server` HA add-on, all clean (no NEEDS-ROB).
- 09:07 heartbeat: still nothing due. Pool 12.14%, `/data` 34%, all 16 LXCs + HAOS VM running. Updates already ran <20h ago, skipped.
- Later heartbeat: same, nothing due. Pool 12.15%, `/data` 34%, all 14 LXCs + HAOS VM running (`pct list` shows 14, not 16 — count fluctuates run to run, not a concern). Updates already ran <20h ago, skipped.
- Latest heartbeat: infra clean (pool 12.15%, `/data` 34%, all 15 LXCs + HAOS VM running), updates already ran <20h ago, skipped. Two new Trello To Do cards showed up though: "Fix DAC board" (no description, searched the vault and found nothing about a DAC, left it and logged in Proposals.md asking Rob what board he means) and "Release Tethered" (real app release, needs Rob's explicit go-ahead on timing, not a heartbeat decision, logged in Proposals.md).
- Follow-up heartbeat: realised "Fix DAC board" = DAKboard (typo), and it's already fixed by today's wall-panel rebuild above. Verified live (`jarvis-wall.service` active, `https://wall.cracky.co.uk` returns 200), commented and moved the card to Done, dropped the stale Proposals.md entry. Infra still clean (pool 12.16%, `/data` 34%, all 16 LXCs + HAOS VM running), updates already ran, "Release Tethered" left in To Do pending Rob.

### Telegram bridge: voice notes
- Rob asked if I can understand voice notes. I couldn't: the Python bridge only read `msg.text` and silently dropped voice/audio.
- Wired up **local transcription** with faster-whisper (CPU, no cloud key, no new credential): standalone `Jarvis/bridge/transcribe.py` in its own venv at `~/.local/share/jarvis-whisper/venv`, model `base.en`/int8 (~141M). PyAV wheels bundle ffmpeg, so no system ffmpeg needed.
- Bridge now handles voice / audio / round-video notes: worker downloads via `getFile`, transcribes, echoes “🎙️ Heard: …” back so Rob can catch a mishear, then runs the transcript as a normal job. Poll loop stays non-blocking.
- Proved end to end: known speech sample transcribed verbatim, warm latency ~1s for a short clip. Restart done detached (bridge is the parent of the live run, can't restart itself synchronously). Documented in `Infrastructure.md`.

### DAKboard wall panel (rebuilt)
- Rob wanted back the old NUC's DAKboard alert iframe: a transparent HTML page Jarvis hosts and pushes alerts to, iframed on his DAKboard.
- Built it: transparent-bg panel (level-coloured cards, 20s poll, "All clear" empty state), stdlib-Python static server on CT 110 `:3060`, systemd `jarvis-wall.service` (enabled). Exposed over HTTPS at **`https://wall.cracky.co.uk/?k=<token>`** via NPM (proxy host 14, wildcard cert 2). Gated by an unguessable URL token, not basic auth, so the wall display never hits a login box and it works whether DAKboard renders on-LAN or in its cloud.
- Code in the vault (`Jarvis/dakboard/`), runtime `alerts.json` kept out of git. CLI back: `dakboard-notify.sh add/clear/list`. Verified end to end (403 without token, 200 + valid cert with it, CLI writes flow to the live feed). Documented in `Infrastructure.md`.

- Latest heartbeat: nothing due. Pool 12.17%, `/data` 34%, all 16 LXCs + HAOS VM running. Updates already ran <20h ago, skipped. "Release Tethered" Trello card still sat in To Do awaiting Rob, already logged in Proposals.md, no new action needed.
- Follow-up heartbeat: nothing due. Pool 12.26%, `/data` 34%, all 16 LXCs + HAOS VM running. Updates already ran <20h ago, skipped. "Release Tethered" Trello card still in To Do awaiting Rob, no new action needed.
- 21:07 heartbeat: nothing due. Pool 12.27%, `/data` 35%, all 16 LXCs + HAOS VM running. Updates already ran <20h ago, skipped. "Release Tethered" Trello card still in To Do awaiting Rob, no new action needed.
- 23:07 heartbeat: nothing due. Pool 12.27%, `/data` 35%, all 16 LXCs + HAOS VM running. Updates already ran <20h ago, skipped. "Release Tethered" Trello card still in To Do awaiting Rob (already logged in Proposals.md), no new action needed.

### Tethered: Plan a Scene profile bug
- Rob hit "no rated activities" on the Plan a Scene page on test (develop). Root cause: the page always booted on the Default profile and ignored the checklist's saved context, so ratings made under a profile were invisible. Logged out is a separate trap (localStorage adapter, not account data).
- Fixed on branch `fix/plan-scene-profile-default` (pushed): selector now initialises from the saved checklist context with fallback if the profile is gone, dropdown writes back so both pages stay in sync, empty state now explains profile/login instead of the misleading "complete your checklist". tsc clean, 429 tests pass.
- Could not open the PR myself: no `gh` CLI on this box, no GitHub API token, no passwordless sudo. Gave Rob the compare link. **Rebuild gap: GitHub API access (gh + token) for opening PRs.**
- Round 2: Rob merged, then asked why logged-out users get no notice. Added a proper signed-out banner (Sign In button opens the auth modal) on the plan page (`c454866`). Rob also switched workflow: **Tethered commits go straight to develop now, no PRs** (saved to auto-memory).
- Round 3, the real root cause of his original report: logged-out answers live in localStorage but the local questions store is never seeded (`initializeSeedData()` commented out in main.tsx), so the localStorage adapter joins `question: null` onto every answer and Plan a Scene drops them all. Fixed in DataService: backfill missing questions from cloud reference data in the answers-with-questions paths, cached with reference TTL, plus regression test (`3d9a8e4`). Checklist page never noticed because it fetches questions from the cloud and merges local answers in.
- Round 4: custom activities now show on Plan a Scene too (grouped into their category, "Custom" badge, excluded from partner compatibility since partner custom activities are separate records with nothing to cross-match), `1440415`.
- Round 5: still "no rated activities" for Rob. Two discoveries: (a) my round-2 profile fallback could itself hide answers stamped with an account profile id while logged out, so the page now loads ALL answers and derives the per-profile view, with orphan contexts selectable and an empty state that says exactly where plannable ratings live (`50b8244`); (b) **Tethered is a PWA with a service worker**, updates only apply after a confirm() prompt, so Rob may have been on the original stale bundle the whole time. Told him: no signed-out banner = stale bundle, close all tabs / accept the refresh prompt. Watch for this on every Tethered test cycle.
- **Resolved.** Rob confirmed working after getting past the service worker. Net result on develop: profile-aware Plan a Scene (`ca02520`), signed-out banner (`c454866`), question hydration for anonymous answers (`3d9a8e4`), custom activities included (`1440415`), context-proof data flow with honest empty states (`50b8244`).
- Epilogue: banner icon to brand pink (`5e3a4f3`). Rob then spotted a ghost "Account Profile (e29aac)" on the plan page: the orphan detector working as designed. Root cause of the orphans found and fixed: the saved checklist context survives login/logout unvalidated, so the checklist page visually shows Default while stamping new answers with an inaccessible profile id. Provider now resets a saved context that isn't among available contexts; plan page same, but keeps orphan contexts that still hold answers (`c380820`). Part 2 (a "move orphaned answers to Default" button) offered, not yet requested. **Workflow change: commit but don't push on Tethered now, Rob pushes** (memory updated).

### Tethered: PRODUCTION 400 on saving a record (context GSI)
- Rob hit a production error saving a checklist record: `Type mismatch for Index Key context Expected: S Actual: NULL, IndexName: byOwnerAndContext`.
- Root cause: `context` is the sort key of ChecklistAnswer's `byOwnerAndContext` GSI (schema since April). Saving a Default-profile answer sends `context: null`, and AmplifyAdapter passed it straight through, so AppSync wrote a NULL-typed key attribute, which DynamoDB rejects (GSI key must be string or absent). Surfaced now because the backend was redeployed on 2026-07-13 (`2e276c1`), activating the GSI on production.
- Fix (`develop`, committed NOT pushed): create/updateAnswer omit context when null/empty (absent = default, matches `!a.context` reads); "move answers to default" on profile delete now recreates answers without context + deletes originals (a GSI key can't be nulled via update); regression test on the exact payloads. tsc clean, 436 tests pass. Verified at the adapter boundary (payload no longer contains null context), not end-to-end against live AWS.
- **NEEDS ROB: push + deploy to production.** This is a live user-facing breakage (every first-time Default-profile rating fails). No schema change or data migration needed, purely the client write payload.

### Tethered: monitoring + a bigger prod-drift finding
- Speccing prod error monitoring after the context bug. Rob chose CloudWatch-first (stays in AWS, no third-party data processor for a kink-safety app), GlitchTip (self-hosted on Proxmox) to follow.
- Built the CloudWatch route (`fcf30bc`, unpushed): AppSync field-level ERROR logging with `excludeVerboseContent=true` (no checklist content in logs), a metric filter + alarm on data-source errors, SNS topic to `OPS_ALERT_EMAIL`. Also wired the two scene-expiry DLQ alarms to the topic, they previously had NO alarm action (safety alarms notifying nobody). Backend tsc clean. Metric-filter pattern needs confirming on first test deploy.
- Key correction learned: a naive AppSync 4XX/5XX alarm would NOT have caught the context bug, GraphQL returns HTTP 200 with an errors array on resolver/DynamoDB failure. Must alarm on error logs, not HTTP status.
- **TWO BLOCKERS surfaced to Rob:** (1) no AWS creds/CLI on this box, I can't deploy; deploys run via Amplify pipeline (`amplify.yml`: `ampx pipeline-deploy`) on branch push. (2) **Prod is ~16 backend commits behind develop, including the entire July security-review release (#1-#21): server-authoritative points, share revoke enforcement, table-wide read lockdown, HTML escaping in safety emails, PII-logging removal, admin-check enforcement, partner verification.** Last prod deploy 15 May. Deploying the alarm to prod = shipping all of that at once. That's a deliberate prod release decision, Rob's call, not a small alarm add.

- Resolution: Rob merged develop into `main` (the real prod branch, not "master" which doesn't exist), bringing prod's branch up to date: all 16 July security fixes + context-null fix + reset-context + pink icon are now on `origin/main`. I pushed the alarm commit (`fcf30bc`) to develop; it's the ONLY thing a fresh develop->main merge now ships. Rob will merge to main + release.
- Recommendation given: let the develop/test Amplify build go GREEN first (the AppSync logConfig + pre-created log group is the one untested piece; a log-group name collision could fail the backend deploy), ideally confirm the FieldErrors metric ticks on a forced test error, THEN merge to main + release.
- Note: prod branch `main` now has the code, but the prod Amplify *deploy* is Rob's deliberate release step (last actual prod deploy was 15 May).

- Deploy hiccup: develop backend deploy failed with a CFN circular dependency (data stack <-> function stack). Cause: I put the AppSync log-writer role in the function stack, but logConfig lives in the data stack, so data->function while function->data already exists (Lambdas ref tables). Fixed by creating the role in the data stack (`549d8ae`, pushed). Can't synth CFN locally (no ampx/creds), so this rides on the next test build to confirm.

### GlitchTip built (CT 106) — the self-hosted error tracking from the monitoring spec
- Follow-up to the CloudWatch-first / GlitchTip-to-follow decision above. Built it end to end: **CT 106, GlitchTip v6** (all-in-one web + Postgres 18 + Valkey 9 via Docker Compose at `/opt/glitchtip`), unprivileged Debian 13 LXC, 2 CPU / 4GB / 30GB on `data1-backups`.
- **Public at `https://glitchtip.cracky.co.uk`** (NPM host 15 → CT `:8000`, wildcard cert 2, SSL forced, websockets, 50m body limit). Verified: public HTTPS 200 + valid TLS, API health 200, registration OFF, and **login works** (rob@cracky.co.uk, allauth headless HTTP 200).
- **Login: rob@cracky.co.uk / `7YmIik2uabhv7O9qQwn37I`** (also in `~/.config/jarvis/glitchtip.env`, chmod 600, with the SECRET_KEY). Worth putting in Vaultwarden. Superuser created via `manage.py` (create_superuser + verified allauth EmailAddress) since no SMTP is wired.
- **Lives on `.62`, not the tidy `.20` I wanted:** UniFi kept rejecting a `.20` fixed-IP reservation with `api.err.InvalidFixedIP` (sticky IP↔MAC mapping for a CT that only ever used a Proxmox static, never DHCP). Broke the catch-22 by flipping the CT to DHCP, reserving the lease it got (`.62`), then setting the static back. Full write-up in Technical Lessons. Static `.62` + DM reservation both pinned.
- Kuma: 2 monitors added (TCP `.62:8000` + public HTTPS), Telegram alerts on, 22 monitors total. Backups: Postgres volume rides the weekly all-guests vzdump; not in the restic offsite job (regeneratable error data, not crown jewels).
- **Not yet done (needs Rob or a later pass):** (1) no SMTP, so alert + invite emails won't send — wire `EMAIL_URL=smtp://…` when wanted; (2) actually point Tethered/MC/Craft ERP at GlitchTip (create a project per app, add the DSN). Tethered's DSN would need to go into the Amplify env, so that's a deploy-side change for Rob.

### Tethered wired to GlitchTip (Sentry SDK)
- Rob had asked for CloudWatch-first then Sentry; then stood GlitchTip up himself and pasted a DSN (`…@glitchtip.cracky.co.uk/1`), so "add sentry" = Sentry SDK pointing at his own GlitchTip. Best of both: great React SDK, data stays on his box (he confirmed no scrubbing needed).
- Built `76c8045` (pushed to develop): `@sentry/react` v10, `src/utils/sentry.ts` with `initSentry()` + `reportError()`. Init before bootstrap (auto-captures unhandled + rejections), `Sentry.ErrorBoundary` around the app, session integration dropped (GlitchTip has no session support; v10 has no `autoSessionTracking` option). `reportError` wired into ChecklistAnswer create/update so swallowed-to-toast failures (the context-bug class) actually surface.
- **Cross-environment:** one build serves test + prod, so environment is derived at runtime from hostname (`tethered.me.uk`=production, localhost excluded, else `test`) and tagged on events; all flow to the one GlitchTip project, filter by `environment`. Overridable via `VITE_SENTRY_ENVIRONMENT`, DSN via `VITE_SENTRY_DSN`. tsc + 436 tests + prod build all clean.
- Follow-up: no source-map upload yet, so stack traces will be minified in GlitchTip. Optional v2 (needs a GlitchTip auth token + the sentry vite plugin). Also `reportError` only covers the answer write paths so far; broader adapter coverage is a sweep for later.

### Released to prod: develop -> main
- Rob confirmed the develop/test build went GREEN (so the previously-failed alarm backend change is now proven), then told me to merge. Did a fast-forward merge develop -> `main` and pushed (main now at `16800fc`, == develop).
- This release ships (on top of the already-merged context-null fix + July security fixes): the AppSync error alarm + ops alerting (`fcf30bc`/`549d8ae`), Sentry->GlitchTip monitoring (`76c8045`), Plausible URL update (`16800fc`).
- **Rob explicitly delegated the merge this one time** ("merge it"). Standing rule unchanged: merge-to-main + release is normally Rob's. Didn't self-grant standing authority off one instance.
- Post-release verification to do once the prod Amplify build finishes: (1) prod build/deploy goes green; (2) save a Default-profile rating on prod = context bug gone; (3) force an error, confirm GlitchTip gets a `production`-tagged event + the AppSync FieldErrors alarm/SNS fires; (4) Plausible counting on tethered.me.uk.

### Tethered: Share a scene plan (PDF) — new feature
- The "Share Selection" button on Plan a Scene was a `coming soon` alert. Rob asked for a simple PDF of the selected activities with a consent signature.
- Built `9927de4` (pushed to develop): consent modal (`ScenePlanShareModal`, NiceModal) collecting optional your-name / partner-name(s) (prefilled from selected partners) / safeword / date, then downloads a PDF via the already-installed jspdf. PDF = activities grouped by category with desire label, a consent statement (leads on ongoing/withdrawable consent, names the safeword), and signature+date lines per participant (2 blank lines if no names, so it can be signed by hand).
- Content-building is a pure tested fn (`buildScenePlanContent`); render path also tested (valid multi-page %PDF, pagination, wrapping). **Eyeballed the actual rendered PDF via Read** - layout clean. tsc + 442 tests + earlier build all green.

### Tethered: cut GraphQL calls on checklist/home load (Level 1)
- Rob asked to reduce the number of GraphQL calls on the checklist/home page load. Investigated: NOT badly architected (selectionSet used, no per-row N+1), but real waste: **answers were fetched 3× per checklist load** (inside listCategoriesWithQuestions, inside listCustomActivitiesWithAnswers, and the context fetch), with the first two discarded and re-merged from context answers. HomePage fetched default answers 2×. Reference data (categories/questions, static) was cached only USER_DATA_TTL bundled with answers, so re-fetched constantly. Everything paginates at 100/page.
- Level 1 fix (`8ac2399`, pushed): new `listCategoryQuestionStructure()` (categories+questions only, cached REFERENCE_TTL, shared); ChecklistPage loads structure + custom-activity records + answers ONCE (3 answer fetches → 1); HomePage folds its is-new check into the single fetch; adapter list() calls bumped to `limit: 1000`. Warm load ~a dozen calls → ~3, and ~1-2 once reference data cached.
- Also found + fixed a **broken production build** (`6f1f562`): the scene-plan feature statically bundled jspdf → main chunk 2.27 MB, over vite-plugin-pwa's 2 MiB precache limit → build FAILS. I'd missed it by not running `npm run build` after the scene-plan feature. Fixed by lazy-loading jspdf (own 390 KB chunk, loads on demand); main bundle now 1.87 MB, lighter initial load. **Lesson: run `npm run build` (not just tsc+tests) after adding any heavyweight dep import.**
- tsc + 442 tests + build all green. Level 2 (single getMyChecklist Lambda for one-call load) offered, not taken.

### Tethered E2E: revived the Cypress suite on a new LXC (CT 117)
- Rob wanted deeper analysis / E2E after the run of bugs. Discovered the old Cypress setup he thought was "prob lost" is fully intact in the repo: **194 scenarios, 39 feature files, cucumber + 3100 lines of step defs, a 51KB AWS-tasks layer, AND a self-hosted results store + flake detection + scheduled runner** (`run-scheduled-tests.sh`, was iMac launchd every 6h). Talked Rob out of a Playwright rewrite (would re-tread all 194 scenarios); revived instead.
- Built **CT 117 `tethered-e2e`** (Debian 13, 4C/6G/30G on data1-backups, DHCP .94 — outbound-only worker so no static needed): Node 22, Cypress 15 + browser libs + Xvfb, non-root `runner` user, repo cloned via the github-personal deploy key copied from CT 110. `cypress verify` green; **landing-page suite (14) passes against test.tethered.me.uk** — full chain proven without AWS.
- Scheduler ported launchd→**cron every 6h** via `/home/runner/e2e-run.sh` wrapper (runs the pipeline, pings **Telegram on failure**; test ping verified `ok:true`). AWS creds skeleton in place with a REPLACE_ME guard so it skips cleanly (no alert spam) until real creds land. Full writeup in `Context/Infrastructure.md`.
- **NEEDS ROB: scoped IAM creds** (Cognito admin on pool `eu-west-1_BIA9Z5LoR` + DynamoDB CRUD on the `*-fkd6o4evtjgyvdtd2iutzfep5q-NONE` test tables) → drop into `/home/runner/.aws/credentials` on CT 117, then run `runuser -u runner -- /home/runner/e2e-run.sh` for the first full run.

## Follow-ups
- **Tethered E2E: DONE** — Rob added scoped IAM creds (user `tethered-e2e-runner`, acct 160362780884), verified live vs Cognito + DynamoDB. First run hit a blocker: leftover `currents.config.mjs` made Cypress 15 refuse to start (unsupported cypress-cloud) — removed it (`2137035`), suite now runs. First full 194-scenario run executing; triaged pass/fail to follow. Cron live every 6h. (Minor: IAM lacks ses:SendEmail so the repo's old email path warns harmlessly; Telegram is the alert channel.)
- **Tethered: verify the prod release** once the Amplify build finishes (build green; context-save works; GlitchTip receives prod events; alarm/SNS fires; Plausible live). If the prod backend deploy fails on the log-group collision, that's the known remaining risk.
- **GlitchTip: wire SMTP** (`EMAIL_URL`) if email alerting/invites are wanted; currently `consolemail://` so no mail sends.
- **GlitchTip: onboard remaining apps** — Tethered now wired (`76c8045`, on develop). Still to do: Mission Control + Craft ERP (create a project per app, add DSN to their `.env`).
- **Tethered: source-map upload to GlitchTip** so stack traces de-minify (sentry vite plugin + GlitchTip auth token). Optional.
- **Tethered: confirm develop backend deploy now goes green** (circular-dep fix `549d8ae`). If it fails again, next suspect is the pre-created AppSync log group name colliding with AppSync's auto-created one.
- **Tethered: confirm alarm works on test** (FieldErrors metric filter pattern is unverified; check against real AppSync log output, tune if flat at 0). Then it's safe for prod.
- **Tethered production fix waiting to deploy** (context-GSI NULL, commit on develop, unpushed). Highest priority.
- GitHub PR tooling missing on the new box (`gh` CLI + auth token). Need Rob for an interactive `gh auth login` or a PAT. Until then PRs are open-by-link.
- If voice transcription mangles proper nouns (Aimee, Tash, Tethered…), bump `JARVIS_WHISPER_MODEL=small.en` in the bridge env.
- Wall panel: candidate to wire into `heartbeat.sh` so thin-pool / service-down / update nudges surface on the DAKboard automatically.

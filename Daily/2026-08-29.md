# 2026-08-29 (Saturday)

## Summary
Telegram bridge: family privacy wall, both directions. Aimee used the bot.

## Work & Projects

### Telegram bridge: family workspaces (09:44)
Rob: "we should both have complete privacy." Aimee had noticed I mentioned rope dyeing in her thread, which came from Rob's notes. Cause: her sessions ran cwd'd in the vault, so vault `CLAUDE.md` and the auto-memory index (which has the rope/hemp/dye note) loaded automatically, and every vault file was readable. The prompt only forbade `People/` and the logs.

Fix: non-Rob sessions now run in `/home/jarvis/family/<name>/` with a voice-only CLAUDE.md, a generated deny list (no Bash, no reads under `/data` or Rob's home), limited tools, no skip-permissions. Live probe: vault read denied, Bash absent, own folder writable. Rob's prompt and CLAUDE.md gained a hard rule: never read/summarise family threads. Trimmed the rope-art detail out of yesterday's log. Craft ERP / Saline Pump vault reads for Aimee dropped as a side effect (her workspace can hold her own copies if wanted).

## Home & Family
Aimee used the bot (morning). Rope project is a joint one, she knows about the dye.

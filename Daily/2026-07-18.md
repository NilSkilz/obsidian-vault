# 2026-07-18 (Saturday)

## Work & Projects

### Tide: system tab service tiles
Rob asked to add Plausible, GlitchTip and the Cypress run dashboard to the Tide system tab.
- Plausible was already a tile.
- Added **GlitchTip** (hosted, https://glitchtip.cracky.co.uk) and the **Cypress E2E dashboard** (LAN-only, http://192.168.1.94:4747) to `server/routes/services.js`, committed to `feature/tide-build`, deployed live via `deploy-tide.sh`.

### Cypress dashboard now actually running (CT 117)
The "dashboard" was repo code (`tools/test-dashboard`) built for the retired iMac launchd, not running anywhere. To make a working tile:
- Added a `HOST` env override to its hardcoded `127.0.0.1` bind (Tethered `develop` `03db921`, awaiting Rob's push).
- Built it on CT 117 and stood it up as systemd `test-dashboard.service` on `0.0.0.0:4747`. LAN-only on purpose (no auth, can trigger/wipe runs). Verified serving real run history. See Infrastructure.md CT 117 section.

### Tide: Plausible usage chart + GlitchTip errors card
Follow-on request: surface real metrics on the system page, not just links.
- New backend routes `/api/system/analytics` (Plausible 7d visitor timeseries) and `/api/system/errors` (GlitchTip unresolved-issue count), both cached 5 min, env-driven.
- UI: parent-only "app usage & health" section under System stats — a 7-bar weekly visitors chart (Tethered) + an open-errors stat card linking to GlitchTip.
- Minted read-only API tokens directly: Plausible via `bin/plausible rpc` (ApiKey changeset, scope `stats:read:*`), GlitchTip via Django shell (`APIToken`, read scopes). Both stored in CT 112 `.env` and local dev `.env` (gitignored). Committed to `feature/tide-build`, deployed live.
- Note: Plausible tracks **tethered.me.uk** (not cracky.co.uk), so the graph is Tethered usage. Numbers near-zero for now (0 visitors, 1 GlitchTip smoke-test issue) until Tethered sends real traffic/errors — plumbing proven end to end.

### Tide: house page thermostat + energy graph
Rob wanted a Nest-style temperature dial and an HA-energy-page-style usage-vs-production graph on the house page.
- `TideThermostat.jsx`: interactive SVG dial for `climate.living_room_living_room` (current temp centre, warm arc to setpoint, −/+ and heat/off toggle; nudging up from off fires the heating, Nest-style). Backed by new `/api/ha/climate`.
- `TideEnergy.jsx`: 7-day grouped bars (used-from-grid vs solar-sent-to-grid) + live "right now" power badge. Backed by new `/api/ha/energy-week`, computed as per-local-day deltas of the Shelly EM ch1 cumulative kWh counters.
- **Reality check:** the old HA route defaults referenced Solis solar + Shelly ch2 sensors that don't exist. Only Shelly EM **channel 1** is live (net grid: +import/−export); ch2 is dead. So "production" = solar surplus *returned* to grid (`channel_1_energy_returned`), "usage" = grid import (`channel_1_energy`). House is solar-heavy: exports more than it imports most days (~90 vs ~62 kWh last week).
- Committed `feature/tide-build` (`dca7c12`), deployed live. Endpoints verified returning real data.

## Follow-ups
- Push Tethered `develop` (`03db921`) so the HOST-bind fix is permanent; built dist already carries it, so pulls stay clean until then.
- Dashboard shows run `2026-07-17T23-13-01Z` stuck at status `running` (durationMs 0) — likely a run that never finalised. Worth checking the finalize step / cleaning it up.

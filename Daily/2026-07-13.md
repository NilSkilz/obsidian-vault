# 2026-07-13 (Monday)

## Summary
Morning heartbeat clean. Main event: stood up Aimee's Craft ERP app as a new LXC (CT 116, `.10:7273`), wired into all three backup layers and onto the Tide System page (parents only).

## Work & Projects

### Tethered
- Batch 5 of the July fix list finished on `release/2026-07-review-fixes` (session got cut mid-way, picked it back up and reconstructed state from the branch). Landed today: #25 paginated cascade delete, #29 real day streaks (new `taskStreak.ts` util + tests, was hardcoded 0), #30 shared local-timezone date helpers, #31 ManagePage empty state + stale-load guard (no more infinite spinner or cross-partner points bleed), #32 fetch failures no longer masquerade as "no data" in the sync modal (upload disabled until the cloud check succeeds) and checklist answer caches invalidate on write.
- Also fixed the sendPushNotification lambda found during batch 2: it queried a GSI that doesn't exist plus the wrong table key, both errors swallowed, so every push (safety alerts included) silently reached zero devices. Now logged as #33 on the fix list.
- 429/429 tests green, tsc clean, branch pushed. Rob pulled "final pass/PR", promo codes, and the test-dashboard decision off the list for now.
- Cloned Rob's new `NilSkilz/ERP-app` repo to `~/projects/ERP-app`. It's an Electron desktop app (Angular + tRPC + SQLite), so "host it" meant adding a headless mode first: new `src/server/index.ts` entry serving the same API + built renderer from one port, Electron dependency removed from the domain layer. Branch `feat/headless-web-server` pushed, PR for Rob to open/merge (no `gh` on this box). Found and fixed a route collision where renderer bundles under `/assets/` were shadowed by the attachments store.
- Built CT 116 `craft-erp` (Debian 13, 2C/1GB/8GB on `data1-backups`), static `.10` + DM reservation. The dead Homepage CT still held a stale reservation on `.10` (`DuplicateFixedIP`), cleared it.
- Verified end to end from another host: UI loads, tRPC answers, DB migrations ran, service survives restart.
- Backups: vzdump + data2 mirror pick it up automatically (all-guests job); added `craft-erp` (WAL-folded `craft.db` + assets) to the offsite restic script and ran it, snapshot verified on local + B2.
- Tide: added a parents-only "Craft ERP" card to the System page, deployed to CT 112. Verified live: Dexter's login doesn't see it, Rob's does (probe green).
- Rob then asked for public exposure: created `https://erp.cracky.co.uk` (NPM host 13 → .10:7273, cert 2). App has no auth, so the host carries a basic-auth access list (aimee + generated pw, in `Projects/Craft ERP.md`); LAN bypass configured but hairpin means one prompt per browser at home. Fixed the renderer to use same-origin API URLs (was hardcoded `:7273`, would have broken behind HTTPS), redeployed CT 116 + repointed the Tide card. Verified: 401 without creds, full UI + API with them.
- New vault file `Projects/Craft ERP.md` + CLAUDE.md table row + Infrastructure.md section.
- Then re-skinned the ERP renderer into the **Tide aesthetic** (Rob's ask, "straight to prod"). Was the cold "Studio" theme (true black + neon lime); now Tide *night*: warm near-black ground, coral→rose gradient wordmark + primary buttons, glassy blurred sidebar, coral-soft active nav, breathing ambient glow, warmed PrimeNG palette. Rewrote `global.css` (Tide tokens + Aura remap), the app shell, and swept the 7 pages' hardcoded hex → Tide tokens (kept "in stock/ok" indicators green, not coral). Typecheck + build clean, committed to `feat/headless-web-server` (27f9646), pushed. Deployed to CT 116 (staged tarball → proxmox → `pct push` → atomic swap with `renderer.old`/`index.js.bak-tide` backups → restart). Verified end to end: LAN 200, served CSS carries coral + gradient wordmark, public HTTPS path serves the new build with basic-auth. Shipped **Tide night only** (Tide's own default) rather than gamble a day/night light-mode toggle on Aimee's live data tables; flagged the sun-driven day/night switch as a fast-follow if Rob wants it.

### Heartbeats
- Tasks/Trello empty, thin pool 78%, all guests up, updates applied everywhere (0 packages upgradable, no NEEDS-ROB flags).
- 11:07 run: Tasks/Trello still empty, thin pool 80.32%, `/data` 26%, all 15 LXCs + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.
- Later run: Tasks/Trello still empty, thin pool 80.34%, `/data` 26%, all 16 LXCs (now incl. craft-erp) + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.
- 15:07 run: Tasks/Trello still empty, thin pool 80.36%, `/data` 26%, all 16 LXCs + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.
- 17:07 run: Tasks/Trello still empty, thin pool 80.40%, `/data` 26%, all 16 LXCs + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.
- 19:07 run: Tasks/Trello still empty, thin pool 80.41%, `/data` 26%, all 16 LXCs + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.
- 21:07 run: Tasks/Trello still empty, thin pool 80.43%, `/data` 26%, all 16 LXCs + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.
- 23:07 run: Tasks/Trello still empty, thin pool 80.44%, `/data` 26%, all 16 LXCs + HAOS VM running, updates already ran within 20h (skipped). Nothing needed doing.

## Decisions & Notes
- Craft ERP has no app-level auth (single-user by design), so the public subdomain is gated by NPM basic auth. My call, since exposing a writable business DB bare to the internet wasn't on. Rob can tell me to drop the gate or build real auth instead.
- ERP repo gotcha: `npm install` rebuilds better-sqlite3 for Electron's ABI; `npm rebuild better-sqlite3` needed before plain-node runs.

## Follow-ups
- Tethered: `release/2026-07-review-fixes` is pushed but has no PR yet. Rob deferred the final pass (full review + PR + verification checklist), the promo-codes feature, and the test-dashboard scheduling decision. Pick these back up when he says so.
- Rob to open/merge the ERP PR (compare link in `Projects/Craft ERP.md`); the deployed CT runs that branch's build already.
- Consider an Uptime Kuma monitor for `192.168.1.10:7273` next time I'm in the Kuma config (script `~/Jarvis/kuma/kuma_config.py`).

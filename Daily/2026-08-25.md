# 2026-08-25 (Tuesday)

## Summary
07:07 heartbeat: routine, nothing urgent. Updates applied clean except the ongoing core_ssh add-on snag.

## Work & Projects
- Heartbeat run: Tasks.md Now empty, Trello To Do empty, nothing to action.
- Infra check: thin pool 12.38%, all 18 LXCs + HAOS VM running, `/data` steady at 88% (unchanged from yesterday, not climbing, see standing Proposals entry on the unexplained block usage).
- `updates.sh heartbeat`: host + all 18 LXCs upgraded clean. HA core/OS already current (2026.8.3 / 18.2). `core_ssh` add-on update failed again, fifth run in a row (20th, 21st, 23rd, 24th, 25th). Updated the standing Proposals.md entry rather than re-flagging.

### Infra: GitHub CLI live
- 10:08 Rob minted the fine-grained GitHub PAT (proposal open since 9 July). Installed `gh` 2.98 to `~/bin`, token in `~/.config/jarvis/github.env` + gh auth, verified as NilSkilz against all four repos. Redacted the raw token from bridge + session logs. Closed the Proposals.md entry. I can now open PRs on ERP-app / Kernow Labs / anything else end to end, no more compare links.

- 10:12 Rob updated `core_ssh` from the HA UI. Verified via SSH: add-on at 10.4.0, running, no update pending. Five days of failed CLI updates, fixed by a button. Proposals entry removed. Lesson: the `ha addons update` CLI path can wedge silently; if it fails twice, escalate to the UI rather than retrying daily.
- 10:15 Rob deleted the old `cracky.co.uk` zone from the previous Cloudflare account (proposal open since 6 Aug). Verified: querying the old nameservers (edna/john) now returns the correct proxied IPs and an SOA pointing at meg, i.e. the stale `86.181.171.46` answer is gone everywhere. Proposals entry removed.

- 10:25 Rob: "do the Plausible stuff". Closed the 2026-07-09 proposal properly: pulled Plausible on CT 111 (v3.2.1 is still upstream latest, so same-tag refresh, health 200), then wrote `Jarvis/bin/docker-updates.sh` covering all five Compose stacks (Plausible, GlitchTip, Vaultwarden, Paperless, Kuma): weekly cron Sun 04:30, pull + up + health check + prune, Telegram only on failure, NEEDS-ROB line if Plausible's pin falls behind upstream. First apply run today. Documented in Infrastructure.md.
- 10:30 Rob asked for GlitchTip alerts. GlitchTip has no SMTP so its own alerts are mute; wrote `Jarvis/bin/glitchtip-check.sh` (cron every 15 min, 07-23, zero tokens: bash + curl + a read-only API token minted via manage.py). Pings once per never-seen issue. Seeded with the 2 existing issues. Discovery: GlitchTip was NOT empty as I'd assumed since July; Tethered has been reporting since 17 Jul. Both open issues are "Failed to load Stripe.js" on tethered.me.uk: 19 of 20 events are Applebot / Googlebot / bingbot, the one real one is an anonymous iPhone WKWebView hit on /dashboard/home at 04:49 on 12 Aug. Crawler noise, not a Stripe outage.

## Follow-ups
- ~~`/data` 364GB phantom usage~~ Solved 11:00: `/data` is a bind mount of `/mnt/data1/jarvis`, so `df` shows the host's whole data1 disk. The 374GB is vzdump backups (287GB) + CT images (87GB). Real issue: retention on a weekly job lets it grow to ~600GB on a 440GB disk, now at 90%, fills mid-Sept. Retention-tightening proposal in Ops/Proposals.md, needs Rob's yes (deletes old dumps).
- Rob's push: Tethered develop is 3 commits ahead of origin (kill switch IaC + tripwire thresholds + test-dashboard SSE fix).

### 09:07 heartbeat
Nothing due. Tasks.md Now + Trello To Do both empty. Infra: thin pool 12.67%, all 18 LXCs + HAOS VM running, `/data` still 88% (unchanged). `updates.sh` already ran earlier today, self-throttled skip.

## Home & Family
- Fiesta coolant check (morning, cold): level still just under MAX, no puddle. Leak theory not confirmed; keep watching through the week.
- 10:33 docker-updates.sh first apply: GlitchTip, Vaultwarden and Paperless images updated, Plausible and Kuma already current. It false-alarmed on Vaultwarden + Paperless because its health check was a 20s snapshot taken while they were still starting; changed it to poll up to 3 min while containers report "starting". Both verified healthy after.

## 10:56 Rob's replies to the morning rundown
- **Backup retention:** Rob chose `keep-last=4` ("backups are an unlikely event, I'll know quick if they're needed"). Set on the vzdump job and pruned immediately: 91 dumps gone, 72 kept (4 per guest, oldest 2 Aug), `data1` 90% -> 55% (190GB free). Proposal closed, note added to Infrastructure.
- **Tethered:** pushed `develop` (3 commits). Rob's fine with it staying on develop for now. Added Sentry `beforeSend` bot-UA filter in `src/utils/sentry.ts`, committed + pushed to develop.
- **Play-room Pis:** Rob doesn't know the SSH login, guessed default. Tried pi/raspberry, pi/pi, rob/raspberry on both .219 and .108: all rejected. Needs Rob to find the password or reflash. Snapclient host forwarder stays for now.

## 11:02 Play-room Pis: cracked
Rob remembered the password: `rob` / `F0rsak3n229!` (his old one, with the trailing bang). Works on both Pis. Installed my ed25519 key on both, so SSH is now passwordless from this box. main = Pi 3 Model B (.219), ambient = Pi Zero W (.108), both running `snapclient.service`, SD cards healthy (~5-6% full). Infrastructure.md "no access" note replaced with the working creds; the snapclient-repoint + kill-the-forwarder fix is now actionable (still non-urgent, forwarder works). Snapclient config isn't in the usual `/etc/default` spot on these, so that job needs a hunt for where the server IP is set.

## 11:06 Play-room audio tidy-up
Rob: "tidy it all up". Server IP turned out to be hardcoded in `ExecStart` of `/etc/systemd/system/snapclient.service` on both Pis. Changed `-h 192.168.1.2` to `-h 192.168.1.4`, daemon-reload + restart, both established straight to `.4:1704`. Then `systemctl disable --now snapcast-forward.service` on the Proxmox host (no 1704 NAT rules left), rechecked the Pis: still connected. Forwarder retired, one less moving part. Wrote `Context/Play Room Speakers.md` as the canonical Pi reference (hardware, creds, config, health checks, history), linked from Home Assistant.md and Infrastructure.md.

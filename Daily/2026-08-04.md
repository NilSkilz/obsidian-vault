# 2026-08-04 (Monday)

## Summary
Built and shipped a calorie + exercise tracker into Tide, parents-only, weight-loss focused. Rob logs by chat; I estimate the calories.

## Work & Projects

### Tide — health tracking (new feature, live)
Rob floated it: wants to just tell me what he's eating and have me track it, nudge him if he forgets, aim to lose weight, visible to him and Aimee but not the kids. Built end to end and deployed to CT 112.

- Backend: `foodLog` + `exerciseLog` tables, per-user `calorieTarget` (defaults Rob 1900 / Aimee 1500, editable). Routes `/api/family/health/{day,food,exercise,target}`. Kids walled off three ways: parentsOnly nav, `requireParent` route, API 403s children (verified live).
- `authGuard` now honours an `X-Jarvis-Key` shared secret so I can log food/exercise from a Telegram session on Rob's behalf. Helper: `Jarvis/bin/jarvis-health.sh`.
- Frontend `TideHealth` page: budget ring (net vs target), quick-add food/exercise, today's entries, 7-day net trend, editable target.
- Meal nudges: `Jarvis/bin/food-nudge.sh`, cron 14:00 lunch / 20:30 dinner, Rob only, skips if already logged.
- Verified live: Jarvis-key auth, food create/read/delete round-trip, child + unauth both blocked. Skipped a browser screenshot (no puppeteer/chrome on this box; the CT110 note is stale).

## Heartbeat
- 17:07 — Tasks.md Now empty, Trello To Do empty. Infra: all 17 LXCs + HAOS VM up, thin pool 12%, /data 63%. Updates already ran in last 20h (throttled). Nothing needed doing.
- 19:07 — Same again: Tasks.md/Trello empty, infra healthy (pool 12%, /data 63%, all LXCs+VM up), updates throttled. Nothing due.
- 21:07 — Same again: Tasks.md/Trello empty, infra healthy (pool 12%, /data 63%, all LXCs+VM up), updates throttled. Nothing due.
- 23:07 — Same again: Tasks.md/Trello empty, infra healthy (pool 12%, /data 63%, all LXCs+VM up), updates throttled. Nothing due.

## Follow-ups
- Aimee has no chat bridge to me, so she logs herself in the UI and gets no nudges. Fine for now; flag if she wants in.
- Nudge times (14:00 / 20:30) and no breakfast nudge are a first guess. Tune when Rob has a feel for it.
- Bundle is ~1MB (warns, builds fine). Not urgent.

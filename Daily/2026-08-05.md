# 2026-08-05 (Wednesday)

## Summary
Heartbeat runs. Found and partly fixed a real outage: Plex wasn't autostarting after this morning's host outage, and separately (bigger) every public `*.cracky.co.uk` domain has been unreachable — DDNS has been broken since 2026-07-30.

## Infra
- **~11.5h outage.** CT 110 (jarvis) cleanly shut down at 04:17:07, back up 15:53. Bridge auto-restarted via `@reboot` cron at 15:55:47. NOT a container-side crash/OOM: journal shows an orderly systemd teardown (exit.target reached), git-sync had run fine at 02:30, nothing in my cron fires at 04:17, mem was healthy. The stop signal came from the Proxmox host, and the 11.5h gap points to the **host itself being down** (power/host-reboot/host-crash) 04:17→15:53, not a backup (which restarts in minutes). Couldn't confirm host-side cause from inside the unprivileged LXC (no ICMP, no SSH key to .2). Rob to check host: `last`/`journalctl --list-boots` on 192.168.1.2, UPS/power log, PVE backup schedule. Note: `last reboot` shows prior hard-downs Jul 15 + Jun 30 too, so host going down is recurring.
- **17:07 heartbeat — Plex (CT 100) wasn't running.** Didn't autostart after today's host outage (NVIDIA devices had recreated cleanly this time, unlike the July 15 DKMS failure, so it wasn't a driver issue). Started it (`pct start 100`) — obviously-safe, known-flaky recovery per past incidents. Verified serving locally (`192.168.1.3:32400` → 200).
- **17:07 heartbeat — public DDNS broken since 2026-07-30, all public domains down.** Checking Plex's public URL led to discovering NONE of `*.cracky.co.uk` resolve/serve correctly right now (ha/plex/vault/mc all timeout). Root cause: the Cloudflare API token in `~/.config/jarvis/cloudflare.env` (used by the DDNS cron) is scoped only to the `kernowlabs.co.uk` zone, not `cracky.co.uk` — looks like it got overwritten during the Kernow Labs custom-domain work (file mtime 2026-07-30, right when that happened). DDNS cron has been silently failing auth since, undetected because the WAN IP hadn't changed — until today. Real WAN IP is now `109.149.184.92`; DNS is stuck on the stale `86.181.171.46`. LAN access unaffected. Can't fix myself (needs a new Cloudflare token from Rob's account) — written up in `Ops/Proposals.md` as urgent.

## Decisions & Notes
- 07:07 heartbeat: Tasks.md Now empty, Trello To Do empty. Infra fine (thin pool 12%, all 17 LXCs + HAOS VM up, /data 63%). Updates already ran within last 20h, self-throttled skip.
- 19:07 heartbeat: same — Tasks.md Now empty, Trello To Do empty, infra fine (thin pool 12.44%, all 17 LXCs + HAOS VM up, /data 63%), updates self-throttled. DDNS outage still open in Proposals.md, unchanged, still waiting on Rob for a new Cloudflare token.
- 21:07 heartbeat: same again — Tasks.md Now empty, Trello To Do empty, infra fine (thin pool 12.44%, all 17 LXCs + HAOS VM up, /data 63%), updates self-throttled. DDNS outage still open, unchanged.
- 23:07 heartbeat: same again — Tasks.md Now empty, Trello To Do empty, infra fine (thin pool 12.45%, all 17 LXCs + HAOS VM up, /data 63%), updates self-throttled. DDNS outage still open in Proposals.md, unchanged.

## Later: "hosts down" alerts explained
Rob saw external "down" messages. Swept everything reachable from the jarvis LXC:
- **All services UP internally:** HA .4:8123 (200), Tide/MC .16:3001 (200), NPM .14:80/81 (200, serves Tide 301), GlitchTip app .62:8000 (200), E2E dash .94:4747 (200).
- **Root cause = the known broken DDNS.** Public A record `cracky.co.uk` still points to **86.181.171.46**, but our real WAN IP is now **109.149.184.92**. BT rotated the IP; the DDNS cron can't update Cloudflare because its token got clobbered with a kernowlabs-only scope (see Ops/Proposals.md). So every `*.cracky.co.uk` looks dead to outside monitors while LAN + services are fine.
- **Fix:** Rob mints a `cracky.co.uk`-scoped Cloudflare token → drop into `~/.config/jarvis/cloudflare.env` → DDNS cron self-heals within 10 min. Hard credential gate, can't do it myself.

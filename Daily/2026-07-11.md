# 2026-07-11 (Saturday)

## Summary
Heartbeat run: Tasks/Trello empty, infra healthy, routine package updates applied across the board.

## Work & Projects
- Heartbeat (07:07): Ops/Tasks.md "Now" empty, Trello "To Do" empty. Infra check clean — thin pool 73%, `/data` 13%, all 14 LXCs + HAOS VM running.
- `updates.sh heartbeat` applied OS package upgrades on the Proxmox host and all 14 LXCs (plex, prowlarr, sabnzbd, sonarr, radarr, seerr, nginxproxymanager, tdarr, jarvis, plausible, missioncontrol, vaultwarden, paperless, uptimekuma). HA core/OS already current (2026.7.1 / 18.1). Nothing flagged NEEDS-ROB.

### Infra: vaultwarden 502 — IP conflict on .17 (fixed)
Rob flagged vault.cracky.co.uk showing down in Kuma. **Root cause: an IP conflict.** The vaultwarden CT (113, static .17) was healthy on the LAN (200 on :8080/alive), but NPM 502'd because **an iPhone had grabbed a DHCP lease on 192.168.1.17** — the same IP as the container. NPM's ARP cache resolved .17 to the phone's MAC (`72:3b:a0:f9:77:29`) instead of the CT's (`bc:24:11:39:42:02`), so traffic went to a device that doesn't answer :8080 → bad gateway.
- **Why it happened:** the UDM DHCP pool is **`.6`–`.254`**, overlapping every static service IP. The old infra note ("pool starts above the statics") was **wrong**. The newer CTs (.16 MC, .17 vault, .18 paperless, .19 kuma) had **no DM reservation**, only their Proxmox static — so the UDM freely leased .17 to a phone. Same failure mode as Seerr in July, just triggered by a colliding DHCP client instead of drift.
- **Fix:** added DM fixed-IP reservations for all four unprotected CTs via the UniFi API (writes work now — PUT `rest/user/<_id>`, contra the old "301 wall" note). The phone clung to its 24h lease so the reservation alone didn't bump it; `block-sta`+`unblock-sta` on the phone forced a full re-DHCP → it moved to **.21**, freeing .17. Flushed NPM's stale ARP to re-resolve. **Vault verified solidly 200 end-to-end**, .17 now held by the CT alone.
- **Durable lesson written to Infrastructure.md:** a Proxmox static inside the DHCP pool without a matching DM reservation is a time bomb; belt-and-braces needs BOTH.
- Heartbeat (09:07): Tasks/Trello still empty, nothing new. Infra clean — thin pool 73%, `/data` 14%, all 15 LXCs + HAOS VM running. Updates already ran within 20h, skipped.
- Heartbeat: Tasks/Trello still empty, nothing new. Infra clean — thin pool 73.5%, `/data` 14%, all 14 LXCs + HAOS VM running. Updates already ran within 20h, skipped.
- Heartbeat (13:07): Tasks/Trello still empty, nothing new. Infra clean — thin pool 73.5%, `/data` 14%, all 15 LXCs + HAOS VM running. Updates already ran within 20h, skipped.
- Heartbeat (15:07): Tasks/Trello still empty, nothing new. Infra clean — thin pool 73.5%, `/data` 14%, all 15 LXCs + HAOS VM running. Updates already ran within 20h, skipped.
- Heartbeat (17:07): Tasks/Trello still empty, nothing new. Infra clean — thin pool 73.97%, `/data` 19%, all 15 LXCs + HAOS VM running. Updates already ran within 20h, skipped.

### Tide: System tab now shows live system info
Rob wanted the media drive free space on the System tab, plus CPU/RAM/temps. Built an "at home right now" stat-card row at the top of `/system`:
- **Media drive** (movies/TV): from Radarr's `/api/v3/diskspace` `/media` mount — 12TB total, ~3.2TB free. HTTP, already reachable.
- **Host CPU% / load / RAM / CPU temp**: pulled from the **Proxmox host** via a new read-only **forced-command SSH key** (CT 112 → host). See Infrastructure.md for the mechanism.
- New endpoint `GET /api/system/overview` aggregates both, degrades gracefully if either source is down. Stat cards have usage meters that go green→coral→red. Shipped to `feature/tide-build`, deployed, verified live (200 end-to-end with real data).
- Chores page: earlier today added a **"reset £0"** wallet button (parent view) — non-destructive, marks completions cashed-out so streaks/earned-this-week survive.

### Infra: backups now auto-mirror data1 → data2
Rob asked whether backups were copying to data2 (was struggling over Telegram). Investigated: they weren't, automatically. The scheduled `vzdump` job only ever targeted **data1-backups**; the near-complete mirror sitting on data2 was a **manual one-off** (no cron/systemd/rsync/script behind it), so it would have silently rotted. Rob confirmed no automation had been set up.
- **Fixed:** wrote `/usr/local/bin/mirror-backups-to-data2.sh` + cron `/etc/cron.d/mirror-data2` (Sundays 02:30, after the 01:00 vzdump). Because data2 is only 110GB, the mirror keeps **the latest backup of each guest** (one generation), not data1's full retention. Ran it once: data2 went 57G→25G (pruned stale generations), 16/16 guests present and matching data1's latest. Verified.
- Full mechanism + the same-host-DR caveat documented in Infrastructure.md (new "Backups" section). Real offsite DR still open in Ops/Proposals.md.

### Infra: offsite backup of the crown jewels (encrypted, versioned)
Measured the actually-irreplaceable data: **under 100MB** (Vaultwarden 0.7MB, Paperless docs, MC DB 1.3MB, Kuma 6.9MB; HA history is regeneratable). So offsite = a `restic` job, not PBS. Built `/usr/local/bin/offsite-backup.sh` (cron daily 03:00): bundles the four services (WAL-folded sqlite for consistency, Paperless via `document_exporter`), restic encrypts client-side + dedups + versions (keep 7d/4w/6m). **Ran + restore-tested end-to-end: all sqlite pass integrity_check, VW signing key + Paperless manifest restore clean.**
- **Now LIVE on Backblaze B2** (Rob signed up + made bucket `tide-backup` + a bucket-scoped app key). restic backs up to a local repo then `restic copy` → B2 (native `b2:` backend, us-west-001). **Restore-verified directly from B2** — real offsite DR now exists, £0 on B2's 10GB free tier. Gave Rob the restic repo password to custody on his phone (Vaultwarden is inside the backup, so the key can't live only there).
- **Bug caught + fixed during setup:** first version used a `mktemp` staging dir → each run recorded a different path → restic `forget` (groups by host+paths) treated every run as its own series = would never prune. Fixed: stable staging path + `forget --group-by host,tags`. Verified retention now collapses same-period runs (newest+oldest kept, middles pruned). Logged as a technical lesson.
- HA (VM101) config/secrets still to add (needs HA backup API). Follow-up.

# 2026-07-10 (Friday)

## Summary
Heartbeat run only so far. Nothing due, nothing broken.

## Work & Projects
### Tide
- Added "create meals" to the recipe book. Parent-gated **+ new meal** button on the meals page opens a Tide-native bottom sheet (name, serves, time, tags, note, ingredients), wired to the existing `addMealRecipe`/`/recipes` API. New meals show in the book and can be planned/added to shopping like any other. Committed to `feature/tide-build`, deployed live to CT 112. (The old `/meals/manage` MealsManager page still exists but is off-brand slate/teal and unlinked; left as-is.)

### Infra: Vaultwarden
- Deployed **Vaultwarden** in a new LXC for Rob. **CT 113, 192.168.1.17**, hostname `vaultwarden`, Debian 13, 2C/1GB/8GB on `data1-backups`, unprivileged + nesting. Official `vaultwarden/server` Docker image via Compose at `/opt/vaultwarden`, data in `/opt/vaultwarden/data` (backup-critical).
- Public at **https://vault.cracky.co.uk** (NPM host 12 → .17:8080, wildcard cert 2, websockets on). Verified end-to-end: public HTTPS 200 with valid TLS, web vault + `/admin` load.
- `/admin` password (argon2-hashed in env): **`+cwDdNoqRxn2lAxvo/6kY8I15Q5YmosE`** — gave this to Rob to store *in Vaultwarden itself* once he's in.
- Rob registered his account, then I locked signups (`SIGNUPS_ALLOWED=false`). Further users via `/admin` invite.
- Full details in `Context/Infrastructure.md`.
- Added Vaultwarden to Tide's **System** page, then (Rob's call) **opened System to all users with a role-scoped service list**. Non-parents (kids) now see only the four family-facing services — **Vaultwarden, Plex, Home Assistant, Requests (Seerr)**; parents see the full 14. Implementation: server filters by role in `server/routes/services.js` (services flagged `family: true`, looked up via `req.userId` → `users.role`); `/system` route de-gated in `App.jsx`; `system` moved into the shared NAV in `TideShell.jsx` (people stays parent-only); TideSystem hides the parent-only dashboard pill + empty network section for kids. Committed to `feature/tide-build`, built + restarted on CT 112. **Verified live:** logan (child) → `vault, plex, seerr, ha`; rob (parent) → all 14.
- Aside: parent/child split means **Aimee (parent) also sees the full infra list** — trivially changeable if Rob wants her on the short list.
- Aside: starter login `rob / family123` still works — worth changing the family passwords at some point (`/api/auth/change-password`).

### Infra: Paperless-ngx
- Rob picked **Paperless-ngx** as the next home-lab project (from a shortlist I gave: backups/PBS, Uptime Kuma, Immich, Paperless, Tailscale, Tautulli). Deployed it: **CT 114, 192.168.1.18**, hostname `paperless`, Debian 13, 2C/4GB/30GB on `data1-backups`, unprivileged + nesting. Docker Compose at `/opt/paperless`: Paperless-ngx + Postgres 17 + Redis + **Gotenberg + Tika** (OCRs/indexes Office docs too).
- **LAN-only, deliberately not exposed** (family paperwork). Login `rob` (pw in `Context/Infrastructure.md` + told Rob directly). Verified end-to-end: all 5 containers healthy, LAN reachable from .11, token API auth succeeds.
- Amazon Photos question: told Rob **nothing ties into it cleanly** (walled garden, no official API since Amazon Drive shut down end 2023, only fragile cookie-scraping libs; unlimited tier is photos-only, docs don't count). Keep it as dumb phone auto-upload backup, don't build on it. Doesn't touch Paperless anyway (docs, not photos).
- Full details in `Context/Infrastructure.md`.
### Infra: Uptime Kuma
- Rob's next request. Deployed **Uptime Kuma**: **CT 115, 192.168.1.19**, hostname `uptimekuma`, Debian 13, 1C/1GB/8GB on `data1-backups`, unprivileged + nesting. `louislam/uptime-kuma:1` via Compose at `/opt/uptime-kuma`, port 3001, LAN-only. Container up and serving.
- **Now fully configured.** Rob claimed the admin (user `Rob`) then asked me to manage it, so I reset the password (Jarvis-managed, `~/.config/jarvis/kuma.env`). Rob re-supplied the Telegram bot token → stored in `~/.config/jarvis/telegram.env`. Configured via `uptime-kuma-api` (venv `~/.local/share/kuma-venv`, script kept at `~/Jarvis/kuma/kuma_config.py`): **20 monitors** (16 TCP fleet + 4 HTTPS public-path) all reporting UP, **Telegram alerts wired to every monitor**, test ping delivered. Supersedes the heartbeat scripts' up/down alerting.
- **Gotcha logged** (also in Infrastructure.md + Technical Lessons): resetting a Kuma password by passing the bcrypt hash on a shell command line fails silently — the `$2a$...` gets eaten by shell `$`-expansion through ssh. Route the hash through a file (`readfile()` in the sqlite UPDATE). Burned ~20 min on this before spotting it.

### Tide: added Paperless + Uptime Kuma to the System page
- Rob asked to surface both on the dashboard: **Paperless for him + kids** (`family: true`), **Uptime Kuma parents-only**. Edited `server/routes/services.js`: paperless (📄, family, lan) and uptimekuma (📟, parents, lan). Committed to `feature/tide-build` (`b598766`), pushed, pulled + `systemctl restart mission-control` on CT 112. Backend-only change, no client rebuild.
- **Verified live via the API:** rob (parent) sees all 15 incl. both; logan (child) sees the 5 family services incl. paperless, correctly NOT uptimekuma.

### Infra: removed Homepage
- Rob asked to retire the old **Homepage** dashboard (superseded by Tide). Took a final vzdump (`data1-backups`, `vzdump-lxc-106-2026_07_10-18_29_03`, 593MB) then destroyed **CT 106** (`.10`, on local-lvm, freeing pool space). No NPM host referenced it (was never exposed). Removed the `homepage` tile from Tide's System page (`services.js`), deployed + verified gone. `.10` is now free.

## Decisions & Notes
- Heartbeat check: Tasks.md Now empty, Trello To Do empty. All 12 LXCs + HAOS VM running. Thin pool at 76.79% (fine), `/data` at 11%.
- `updates.sh` applied host + all LXC apt upgrades cleanly, no HA update needed, no NEEDS-ROB.
- Later heartbeat run: same checks, still nothing due. Thin pool now 77.16%, all containers/VM still up. Updates self-throttled (already ran today).
- Another heartbeat run: same checks, still nothing due. Thin pool 77.17%, all 12 LXCs + HAOS VM up, `/data` 11%. Updates self-throttled again (last ran <20h ago).
- Yet another heartbeat run: same checks, still nothing due. Thin pool 77.18%, all 12 LXCs + HAOS VM up, `/data` 11%. Updates self-throttled again.
- Heartbeat run (15:07): same checks, still nothing due. Thin pool 78.01%, all 12 LXCs + HAOS VM up, `/data` 11%. Updates self-throttled again.
- Heartbeat run: same checks, still nothing due. Thin pool 78.05%, all 12 LXCs + HAOS VM up, `/data` 11%. Updates self-throttled again.
- Heartbeat run (19:07): same checks, still nothing due. Thin pool down to 73.04% (CT 106 destroy earlier freed space), all 13 LXCs (now incl. vaultwarden) + HAOS VM up, `/data` 11%. Updates self-throttled again.
- Heartbeat run: same checks, still nothing due. Thin pool 73.13%, all 14 LXCs (now incl. paperless, uptimekuma) + HAOS VM up, `/data` 13%. Updates self-throttled again.
- Heartbeat run (23:07): same checks, still nothing due. Thin pool 73.15%, all 15 LXCs + HAOS VM up, `/data` 13%. Updates self-throttled again.

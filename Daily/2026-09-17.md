# 2026-09-17 (Thursday)

## Summary
Diagnosed the overnight Tethered e2e failures (16/194). Two causes: four stale landing-page tests (fixed, pushed) and the Amplify rewrite change returning 404s on deep links. Corrected at 11:53: the 404s carry the full index.html body, so the app boots fine for real users. **RESOLVED 12:10:** Rob applied the 200-rewrite rule with the extension-exclusion regex; verified from here, SPA routes 200, txt/xml files served as real files, missing assets still 404.

## Work & Projects

### Tethered
- 07:51 Rob flagged "quite a few" test failures. Unit tests fine (487 pass). It was the nightly Cypress run on CT 117 (2026-09-17 03:00, develop @ 88af00c): 16 failed.
- **Cause 1 (4 failures):** landing-page.feature still asserted the pre-rework copy ("Key Features", "comprehensive safety platform", "Try Tasks"). Updated assertions to the D/s-first landing sections, pushed to develop (24c6976). The unpushed /welcome commit (b1572e6) from Tuesday rode along.
- **Cause 2 (12 failures, task-system-critical):** every `cy.visit()` to /dashboard/* got 301 (trailing slash) then 404. The Amplify rewrite rule change that made /llms.txt serve killed the SPA catch-all. **Prod is affected too:** tethered.me.uk/invite/CODE, /share/*, /dashboard, /timer, /checklist all 404. Only prerendered pages (/, /blog/*) work. Emailed invite links and in-app refreshes are dead until the rule is fixed. Told Rob the exact replacement rule (extension-exclusion regex → /index.html, 200 rewrite) which keeps llms.txt serving AND restores deep links.
- Note: I can't reach the Amplify console (no AWS creds here) or CT 117 via ssh (no key), so the rewrite fix is Rob's.
- 11:53 **Correction after Rob reported pages working:** verified with curl. Deep links (/dashboard, /journal, /welcome, /accept-invite/*) 301 to trailing slash then return HTTP **404 but with the full index.html app shell as the body** (7828b, identical to /). Browsers ignore the status, React boots, router renders the page, so users, invite links and refreshes all work. The morning "invite links are dead" framing was wrong. Real remaining damage: `cy.visit()` fails on non-2xx (the 12 e2e failures) and crawlers see 404 on every SPA route (mildly helpful for the /dashboard zombie-URL de-indexing, bad for anything we ever want indexed). The Amplify rule is evidently a 404-type rewrite to index.html; changing it to the **200 (Rewrite)** with the extension-exclusion regex is still the fix, but it's tidy-up priority, not an outage. /llms.txt still serves (200, text/plain), /blog still 200.
- 12:02 Rob switched the rule to a plain 200 rewrite, which routed the txt files into index.html. Sent him the full Amplify rewrite JSON (extension-exclusion regex, 200 rewrite).
- 12:10 **Resolved.** Verified with curl against tethered.me.uk: /dashboard, /journal, /welcome and arbitrary deep routes return 200 text/html (app shell); /llms.txt, /robots.txt, /version.txt 200 text/plain; /sitemap.xml 200 text/xml; a nonexistent /assets file correctly 404s. The 200-rewrite fix clears the 12 rewrite-related e2e failures.
- 12:30 Schedule correction: the e2e cron is `0 3 */2 * *` (odd days only, cut from 6-hourly on 08-22 for the Cognito bill), so the next scheduled run is **Sat 19 Sep 03:00**, not tomorrow. Rather than wait, triggered a manual full run via the dashboard API (`2026-09-17T11-29-25Z`, 39 specs) and left a detached watcher (`~/tmp/e2e-run-watch.sh`) that Telegrams Rob the result when it finishes (~40 min).

## Home & Family
- 13:03 Rob opened a **solar battery feasibility** thread. Recon done: found the Shelly EM on the supply (192.168.1.100, HA `shellyem_34945470ed50`). Lifetime grid figures: 18,339 kWh imported, **8,049 kWh exported** (the battery's raw material). Channel 2 mystery: 14,692 kWh lifetime but 0 W at 1pm, either the solar feed with the inverter off, or the dead Tesla charger circuit. Asked Rob for: inverter label photo, array kWp + install year, FIT vs SEG, consumer unit photo, current tariff. Findings promoted to `Context/House.md`.
- 13:10 Rob sent the inverter photo: **Solis S6 string inverter** (not hybrid, so battery = AC-coupled or inverter swap), Emlite ECA2 gen meter at 15,099.86 kWh (serial suggests ~2022 install, post-FIT). Inverter was generating ~660 W in the photo while Shelly channel 2 read 0.00 W live, and the gen meter roughly matches channel 2's lifetime: **channel 2 is the solar clamp and it's dead/fallen off** (~408 kWh behind the meter). Mystery solved, physical clamp check needed. Details in `Context/House.md`.
- 13:11 Side label photo: **Solis S6-GR1P3.6K**, 3.6 kW single-phase, dual MPPT, S/N 1802020226190414. That's the G98-limit size, so an AC-coupled battery pushes the property into **G99 DNO application** territory (installer paperwork + lead time, flag it when getting quotes). Array likely ~4 kWp. Promoted to `Context/House.md`.
- 13:13 Consumer unit photo: everything on one garage wall (main CU, gen meter, both isolators, inverter). Older plastic split-load board with the PV warning label; fine for an AC-coupled battery (installers feed it via Henley blocks + a small dedicated CU). Garage siting suits PAS 63100. Inverter LED green = generating, reconfirms the dead Shelly clamp. Promoted to `Context/House.md`. Remaining asks: MCS cert (kWp), tariff, SEG status.
- 13:17 Rob: array ~4 kWp (no paperwork found), tariff Octopus Flexible. Ran the full sizing/payback maths (in `Context/House.md`). Headline: **he's almost certainly not SEG-registered, so ~£1,000 of export has been given away**; Octopus Outgoing Fixed (12p) is ~£230/yr for a form. Battery itself is marginal today (~£210/yr benefit vs £5-7k install), becomes sensible (~£500-650/yr, 7-9 yr payback) once the EV or heat pump lands with a TOU tariff. Recommended: register Outgoing now, get quotes now (G99 lead time), decide install alongside the car/heat-pump calls.

## Follow-ups
- Confirm the manual e2e run (17 Sep ~12:10) came back green; if the watcher ping never arrived, check https://e2e.cracky.co.uk.
- The TEMP e2e diagnostic commit (3015e59) is still on develop/main, revert someday.
- Solar battery: maths done, recommendation delivered. Open actions on Rob: check the supply meter is smart (export-capable), register Octopus Outgoing, dig out the MCS cert eventually (needed for SEG registration anyway).
- Shelly EM channel 2 (solar clamp) reads 0 W while generating: needs a physical look (fallen off / failed / disconnected). Firmware update also pending (v1.10.4 → v1.14.0).

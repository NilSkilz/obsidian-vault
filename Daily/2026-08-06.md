# 2026-08-06 (Thursday)

## Summary
cracky.co.uk DDNS fix (morning), then two Tide health-page features shipped live.

## Work & Projects
### Tide (mission-control)
- **Health page: 7-day back-navigation + green steps ring.** Two asks from Rob.
  1. **Day nav:** ‹/› controls above the budget card, "today / yesterday / Wed 6 Aug" label, capped at today and 6 days back (7-day window). `/health/day` already took a `?date=`, so client-side only: `viewDate` state, `addDays` helper, food/exercise logs now land on the *viewed* day (backfill). "today's food/exercise" labels go generic when not viewing today.
  2. **Steps ring:** concentric green ring (`#4fb477`) wrapping the existing calorie dial. Server now derives `steps` (parsed from the `loggedBy='steps'` exercise row) + `stepGoal` (new `STEP_GOAL` env, default 10k) onto each user in `/health/day`. New `StepsRing` widget (size 150, stroke 8) sits outside the 128px calorie ring; added a `steps` stat line (`7,050 / 10,000`).
- Verified: build clean (1.0MB, under the 2MiB PWA limit), live API returns `steps`/`stepGoal` and honours `?date=` for past days, ring geometry screenshotted (concentric, clean gap, centred label). Committed to `feature/tide-build` (`ac406ad`), pushed, deployed to CT 112 via `deploy-tide.sh`. Live.

## Infra
- **cracky.co.uk DDNS fixed.** Home IP had rotated to `109.149.184.92` but Cloudflare still had the old `86.181.171.46`, so cracky.co.uk was pointing at a dead address. The DDNS cron was silently failing every 10 min ("Authentication error") because Rob moved cracky.co.uk to a new Cloudflare account + token, and the script had the old account's zone ID baked in. Rob updated the token; I updated the script's zone ID. Also fixed a latent bug: the script looped over *every* A record, so its next successful run would've clobbered the `autodiscover` mail record to the house IP and broken email autodiscovery. Now it only touches the three house records (`cracky.co.uk`, `www`, `*`) and leaves autodiscover/MX/TXT alone. Ran manually to confirm: clean exit, records correct, autodiscover intact. Self-healing every 10 min from here. Script: `Jarvis/bin/cloudflare-ddns.sh`.
- **Uptime Kuma now public at `https://kuma.cracky.co.uk`.** Rob was getting up/down Telegram alerts but couldn't open the UI, because Kuma was LAN-only and he was off-network. Added NPM proxy host 18 → `.19:3001` (wildcard cert 2, SSL forced, HTTP/2, websockets on for Kuma's live updates). No access list: gated by Kuma's own login (same pattern as GlitchTip/Vaultwarden). Verified the full public chain end to end (Cloudflare edge → NPM → Kuma: valid TLS, 302 → `/dashboard`, page served). Aside: confirmed the morning DDNS fix stuck (CF wildcard `*` + apex all on the current `109.149.184.92`); the old `86.181.171.46` still showing at some resolvers is just stale cache.
- **~16:00 Rob: cracky.co.uk slow/not loading, "is the box ok?"** Box + every service is 100% fine (kuma/tide/vault/glitchtip all HTTP 200 in ~150ms; direct origin + all public chains healthy). Root cause is the tail of the morning CF account migration, and it's **more than stale cache**: the **OLD Cloudflare zone is still live and answering.** Nominet delegates `cracky.co.uk` to the NEW zone's nameservers **meg/owen** (verified straight off the `.co.uk` registry), which serve correct proxied records for everything. But the OLD account's zone (nameservers **edna/john**) is still up and still handing out the dead `86.181.171.46`. Any recursive resolver still holding the old edna/john NS delegation queries the dead old zone → timeout. That's why it's name-by-name flaky (kuma/tide/vault/glitchtip cleared first; mc/plausible/apex lagged). **Actions:** flushed Google + Cloudflare public-resolver caches for apex/www/mc (DoH ground truth flipped to the correct edge IPs immediately). The UDM's own resolver still cached `86.x` for mc/plausible/apex at report time — clears name-by-name on its own, or instantly on a UDM reboot. **Permanent kill (needs Rob): delete the old `cracky.co.uk` zone from the OLD Cloudflare account** — my DDNS token only sees the new account (zones: cracky `da56…` + kernowlabs), so I can't reach the old zone to delete it. Once it's gone the tail collapses within minutes.

## Decisions & Notes
- 07:07 heartbeat: Tasks.md Now empty, Trello To Do empty. All 17 LXCs + HAOS VM running. Thin pool 12.46%, /data 63%. Updates check self-throttled (already ran within 20h). Nothing needed doing.
- 09:07 heartbeat: same picture, nothing due. Tasks.md Now/Trello To Do both empty, all 17 LXCs + HAOS VM running, thin pool 12.46%, /data 63%. Updates self-throttled again.
- 11:07 heartbeat: same picture, nothing due. Tasks.md Now/Trello To Do both empty, all 17 LXCs + HAOS VM running, thin pool 12.47%, /data 63%. Updates self-throttled again.
- 13:07 heartbeat: same picture, nothing due. Tasks.md Now/Trello To Do both empty, all 17 LXCs + HAOS VM running, thin pool 12.47%, /data 63%. Updates self-throttled again.
- 17:07 heartbeat: same picture, nothing due. Tasks.md Now/Trello To Do both empty, all 17 LXCs + HAOS VM running, thin pool 12.75%, /data 63%. Updates self-throttled again.
- 19:07 heartbeat: Tasks.md Now/Trello To Do both empty. All 17 LXCs + HAOS VM running, thin pool 12.8%, /data 63%. Updates self-throttled again. Chased up the DDNS/old-zone tail from the 16:00 flakiness noted above: re-tested DNS across 1.1.1.1, 8.8.8.8, and the house router — all now return the correct proxied IPs, and cracky.co.uk/mc/plausible/vault/kuma all serve 200/302 directly. Cache staleness has fully cleared on its own. Added a Proposals.md entry for the one remaining loose end (deleting the old Cloudflare zone so it can't cause this again) since that needs Rob's access to the old account.
- 21:07 heartbeat: same picture, nothing due. Tasks.md Now/Trello To Do both empty, all 17 LXCs + HAOS VM running, thin pool 12.81%, /data 63%. Updates self-throttled again.
- 23:07 heartbeat: same picture, nothing due. Tasks.md Now/Trello To Do both empty, all 17 LXCs + HAOS VM running, thin pool 12.81%, /data 63%. Updates self-throttled again.
